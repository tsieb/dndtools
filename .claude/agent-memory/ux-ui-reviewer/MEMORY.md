# UX/UI Reviewer Memory Index

No durable cross-session notes yet. Record reusable UX-review findings here — recurring
component anti-patterns, surface ownership maps, spec-to-component mappings — as they emerge.

Governing UX/design references live under `docs/development/` (UX_GUIDELINES, ACCESSIBILITY)
and `docs/architecture/` (DESIGN_TOKENS, INFORMATION_ARCHITECTURE, NAVIGATION_CONTRACT).

## gm-react design system
- [gm-react DS map + contracts](reference_gm_react_ds.md) — where ds components live (apps/gm-react/src/ds), Dialog/Toaster/EmptyState/VisibilityChip contracts, screen-kit T.* token map.
- [Completion-pass UX anti-patterns](project_completion_pass_ux_patterns.md) — recurring gm-react issues: destructive ops w/o confirm/undo, async false-negatives, VisibilityChip gaps, hand-rolled controls; which surfaces are exemplary.
- [Player-surface audit (/play, /join)](project_player_surface_audit.md) — player cluster map + recurring classes: join flow spans SessionPanel too, chrome-less routes lose DS contracts, live regions mounted with content, two DM-only visual languages, phone nav DOM order. Includes verified NON-defects.
- [Beta-readiness audit (2026-07-14)](project_beta_readiness_audit.md) — structural gotchas: HashRouter vs location.assign, divergent list filters across screens, player-safe copy leaking into DM empty states, focus-on-open lands on Skip, /play excluded from responsive gate.
- [DS-layer audit (re-verified 2026-07-31 @ e702bb6f, run #18)](project_ds_layer_audit.md) — `e702bb6f` CLOSED run #17's whole top-3 (6px Slider, unguarded Dialog/Sheet focus restore, missing Button `accent`) + SegmentedControl's inert ellipsis. 30 open, led by ⭐ `IconButton.jsx:49-50` hover PERMANENTLY discarding a caller's `style.color` (live on the player-facing `PlayerView.tsx:436`), and by `Avatar ring="turn"` being the SAME hex as the focus ring so 4 avatars look permanently focused. ⚠️ Premise correction: `IconButton size="sm"`=28px PASSES 2.5.8 — it's a density-contract issue, not a11y. Retires 6 non-defects (shimmer keyframes, nav landmark dupes, Chip onRemove, DataTable sortable, Field htmlFor, label-less StatusDots).
- [Character/encounter cluster (re-verified 2026-07-31 @ 21e4f86e, run #20)](project_char_encounter_cluster.md) — `33651613` CLOSED the silent-writes, dispatch-catch, Prepared-pill and EncounterBuilder-orphan items. 22 open, led by the announcement fix itself LYING at the HP boundaries (`Characters.tsx:433-439` "Damaged 7. 0 of 24" at 0 HP + a durable no-op write) and never re-announcing identical text. ⚠️ New spec `character-sheet.spec.ts:134-186` pins `#main-content` `role=status` to EXACTLY 1 node and full-string-matches its text.
- [Map editor cluster (re-verified 2026-07-31 @ e702bb6f, run #21)](reference_map_editor_cluster.md) — EditorCanvas's ~14 lying announcements, the phone `<h1>` squeeze and 3 Popover `triggerRef`s are CLOSED. 25 open, led by ⭐ `EditorCanvas:280-301` hold-Space having NO `blur` reset (Alt+Tab while held strands a z9 grab overlay over the whole canvas) and no `preventDefault` (Space re-fires the focused button); then `keyboard.ts:180-211` clearing the selection on a REJECTED delete while Marquee can only ever select POIs/tokens. `LayersPanel:64/:321` still announce unconditionally — the exact class EditorCanvas just fixed.
- [Shell + board/scene cluster (re-verified 2026-07-31 @ e702bb6f, run #21)](project_shell_board_scene_cluster.md) — run #15's S/M/L-vs-canvas disagreement + the two hard-disables are CLOSED. 27 open, led by ⭐ `styles/index.css:84` gating the canvas touch-target compensation on `html[data-android]` while the 0.48 scale applies EVERYWHERE ⇒ ~15px operate buttons on any non-Android phone; by three `aria-disabled` controls whose onClick is UNGUARDED (one makes `scene-cards.spec.ts:243-247` a false green, one toasts a raw UUID); and by `Board.tsx:439` `display:none` removing its own "permanent" live region from the a11y tree. ⚠️ NON-defects retired: `--density-touch-target` IS defined; `Seg` Home/End math is correct.
- [Settings/Extensions/Community/WikiReader cluster (re-verified 2026-07-31 @ 21e4f86e, run #13)](project_settings_extensions_cluster.md) — Seg/PREV_THEME_KEY, transcript tabIndex+log, srOnly speakers, Ext scrollers, rail touch-token and the Policy badge all CLOSED. 16 open, led by ⭐ the phone settings `<Select>` naming the WRONG section for any gated tab (CommandCenter → Permissions hits it at the default `core` tier) and the AI Cancel button that is observably inert.
- [Audio/Upgrade/ScenesCreator/ConnectedSources/screen-kit (re-verified 2026-07-31 @ 21e4f86e, run #13)](project_audio_upgrade_scenes_cluster.md) — the phone source-row crush is FIXED. 14 open, led by `ScenesCreator`'s rejection span being invisible to AT when DS `Field error=` already does it right, and `Audio.tsx:1584` hard-disabling every `Bind` in the desktop app. ⚠️ `deletePreset` is PERMANENTLY unfixable (confirm breaks a spec; undo is impossible because save-preset re-captures) — stop re-opening it.
- [Knowledge/WikiReader/Graph/Atlas cluster (re-verified 2026-07-31 @ 21e4f86e, run #13)](project_knowledge_wiki_graph_atlas_cluster.md) — overwrite-import confirm, push-success toast, non-author EmptyState and the Graph disabled-reason hint are all FIXED. 28 open, led by BLOCKER: the Atlas "live to players" StatusDot is UNREACHABLE for the DM (`deliveredMapIdsForActor` keys on the viewing actor; `session.project-active-map` rejects DM targets), so the whole indicator + its new sr-only label is dead code on the only surface that shows it. ⚠️ Graph "Connections (N)" does NOT contradict degree — both are post-filter; only the copy lies.
- [Campaign/Session/CommandCenter cluster (re-verified 2026-07-31 @ e702bb6f, run #21)](project_campaign_session_hub_cluster.md) — `21e4f86e` CLOSED run #15's whole top-4 (standby-teardown confirm, phase announcements, CommandCenter headings, Campaign save toasts). 30 open, led by ⭐ `SessionHeader` being the ONLY control on /session with no `previewing`/`isDm` (a previewing DM gets the full "End the live session?" danger dialog, then a read-only rejection); by parchment `T.ter` on `--color-bg` = **4.01:1** which run #17 promoted into the four CommandCenter `<h2>`s (→ `GrayText` under forced-colors); and by EncounterBuilder's Start whose `aria-disabled` makes `Button.jsx:87` drop onClick, so its own `setError` is DEAD CODE. ⚠️ Retired non-defect: the Factions EmptyState is NOT a dead end.
- [Player/PlayerView/Join cluster (re-verified 2026-07-31 @ 21e4f86e, run #20)](project_player_char_scene_display_cluster.md) — death-save pips, journal pill and /play empty-stage contrast all FIXED. 25 open, led by ⭐ marching order being MOVE-UP-ONLY with the control OMITTED on row 1 (both `SceneCardsPanel` and `Atlas` ship up+down), "Clear" wiping it with no undo beside a sibling that HAS one, and `Player.tsx` announcing ONLY HP while ~12 other durable writes stay silent. ⚠️ `equipment.spec.ts:115` FORBIDS a confirm on Remove — use an Undo toast.

- [App-shell chrome cluster (re-verified 2026-07-31 @ e702bb6f, run #21)](project_onboarding_viewas_cluster.md) — ViewAsControl roving/menuitemradio, the Onboarding ack `aria-invalid`+`role=alert`, and Continue's hard-`disabled` are CLOSED. 14 open, led by the Continue fix being HALF done (a mistyped E2EE ack still reads plain "Continue" with the reason only in a hover `title`), Onboarding STILL having ~zero live regions, and ProjectionControl's compact form hiding the entire workflow pill behind an `audio-off` speaker glyph. ⚠️ `getByRole` name matching is SUBSTRING — "Continue" also matches "Choose an option to continue".

## Cross-cluster mechanisms (check BEFORE filing a form/label finding)
- **DS `Field` (`ds/components/forms/Field.jsx`) auto-associates its label** via `React.useId` +
  `cloneElement` (`:12-21`) — a `<Field>` whose single child has no `id` is NOT an unlabelled control.
  It also renders `error` as `role="alert"` + `aria-invalid` + `aria-describedby` (`:75-85`), describing
  `help` and `error` separately. **This is the fix mechanism for every "Toaster-only validation" finding.**
- ⚠️ **The agent definition points at `…/dndtools-review-loop-ctl/.claude/agent-memory/…` — that path does
  not exist.** Memory lives under `/home/trinkle/Programming/dndtools-review-loop/`.
- Repo-root `tests/e2e/` does NOT exist. All specs are at `apps/gm-react/tests/e2e/`.

## Token-layer landmines (read BEFORE writing any color/theme finding)
- **`--color-visibility-dm{,-subtle}` are UNDEFINED** yet written at 7 sites in PlayerView/Player/Community → DM-only banners lose their border AND tint. Real names: `--color-dm-only-badge{,-subtle}`. [[ds-layer-audit]] item 9.
- `:root` `--layer-*` are LIGHT (tuned for the dark map well); only parchment re-cut them DARK. So a white glyph on them (POIMarker) breaks in the DEFAULT theme, not parchment — check which theme before claiming contrast.
- No on-fill foreground token exists for status colours (`-text` is for `-subtle` backgrounds), so `Button` danger's `#fff` can't be fixed by swapping in an existing token.
- **MEASURED run #20, stop re-filing these two:** `--color-status-warning` used AS TEXT on `--color-surface` PASSES 1.4.3 (dark #f0a830/#1f1810 = 8.65:1; parchment #9a6310/#fdf8f0 = 4.76:1). `--color-accent-foreground` on a `--color-status-success` FILL also passes (dark 8.23:1, parchment 5.38:1).
- **MEASURED run #21 — `--color-text-tertiary` (`T.ter`, the `eb` label colour) is ONLY AA on `--color-surface-raised`.** Parchment `#837057`: on `--color-bg` `#f3ebdd` = **4.01 FAIL**, on `--color-surface` `#fdf8f0` = **4.49 FAIL (marginal)**, on `--color-surface-sunken` = 3.54 FAIL, on raised `#ffffff` = 4.75 PASS. Dark `#9d8d75` passes everywhere (bg 5.86 / raised 4.89). So: file `T.ter` contrast ONLY when the text sits on bg/surface/sunken, and ONLY for parchment. Under `forced-colors: active` `colors.css:379` maps it to **`GrayText`** — never use `eb`/`T.ter` for a heading or any structural label.
- **`--layer-terrain` IS NOW DECLARED** (colors.css:267/:304/:416) as of run #10 — the old "undefined, Grass==Forest" claim is DEAD. Guard: `app/map/mapVocab.test.ts` (compares swatch STRINGS, so near-identical rendered colours still slip through).
- ~~`isolateModalSiblings` inerts the `ToastViewport`~~ **FIXED at 9aeebdde** — `modalIsolation.ts:23` now skips any sibling with `data-modal-exempt`, which `ToastViewport` sets. Toasts raised from inside a Dialog/Sheet/MapEditor are live again. Do NOT re-report.
- **The mobile Playwright profile is `devices['Pixel 5']` = 393×851, NOT 375.** Phone content box after `Page` padding ≈ 365px; inside a `Panel` (pad 18 + border) ≈ 327px. Use these numbers before claiming a `minmax(Npx,1fr)` grid overflows — 260px and 300px tracks both fit.
- **NO global `button:hover` exists** — `src/styles/index.css` contains 0 occurrences of "hover". Every inline-styled `<button>` in a screen has ZERO pointer feedback unless it hand-rolls `useState` hover (`Knowledge.tsx` `RelRow` is the in-repo pattern). This is a systemic finding, not a per-site one.
- ~~`touchAction:'none'` on the read-only `MapBuilder` well~~ **FIXED at 016b696c** — `atlas.spec.ts:134-138` now pins `touch-action: pan-y` on `[data-testid="map-canvas-well"]`. Do NOT re-report. (The general rule still holds: `clippedControls` is X-axis + viewport-relative, so no gate sees a vertical scroll trap.)
- **`Icon.jsx:324` `dm-only` → `VenetianMask` is FIXED** (was `Eye`, same glyph as `visibility-players`). The compact-`VisibilityChip` colour-alone class is CLOSED across all ~33 sites. Do NOT re-report.
- **Motion is handled globally** — `styles/index.css:255-265` zeroes `animation-duration`/`iteration-count` under `[data-motion='reduced'|'none']` and `tokens/spacing.css:143-153` zeroes every `--duration-*`. Never file a per-component prefers-reduced-motion finding.
- **Measure WCAG 2.5.8 with the *Spacing* exception before filing.** A 23.8px-tall chip in a `gap:6` row PASSES if adjacent centers are ≥24px apart — axe's `target-size` (in AXE_TAGS, `wcag22aa`) agrees. Burned once on `Graph.tsx` facet chips.
- Dead DS exports, ZERO consumers — don't spend fix effort: `Tooltip` (its clipping bug is therefore latent), `NavSidebar`/`NavItem`, `DataTable`'s `sortable`, and the `colors.css` legacy alias bridge (`--bg/--fg/--card/…`). `DataTable`'s missing overflow wrapper IS live, though.

## Tooling gotchas
- [DesignSync availability](reference_designsync_availability.md) — DesignSync can be runtime-disabled in ux-ui-reviewer *subagent* context despite being on the toolset; report up + run from main thread.
