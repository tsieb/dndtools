#!/usr/bin/env bash
# CENTRAL_DISPATCHER_ENTRYPOINT: worker ownership belongs to the shared operator.
echo 'This project loop is retired. Start workers through agent-dispatcher; use the unified dashboard or dispatch.py control.' >&2
exit 2
