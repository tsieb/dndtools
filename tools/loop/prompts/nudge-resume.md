Your previous attempt was interrupted — a usage limit or the attempt cap — not finished. Nothing was
thrown away: same session, same worktree. Re-read your journal at {{JOURNAL}} and reconcile it
against `git status --porcelain`, `git diff --stat`, `git log --oneline -5`, then CONTINUE from where
you left off. Do not restart, do not revert done work. Verify synchronously in the foreground, finish
with green commits, keep the journal current. Story: {{ITEM_ID}}.
