# Completion — UX-CONTENT-visibility-and-embeds

UX workpack status: `complete`

Epic: Content Visibility and Embeds (phase "07 Collaboration and Permissions", P1).
Requirement coverage: `UX-CONTENT-016` (section/field visibility), `UX-CONTENT-017` (embeds).

## Summary

Token-styled the granular-visibility + embeds surface (`ContentVisibilityEmbeds`) for visual cohesion,
preserving every functional `ve-*` testid via scoped `:global(...)` selectors over the unchanged
markup/logic.

- The briefing host renders as a token card; the per-section 3-state visibility toggles (DM-only choke
  point), the visible-sections/fields lists, and the embedded-content list become consistent token
  rows. An embed the viewer cannot see keeps its single non-leaking "unavailable" placeholder (now in
  a muted, italic treatment).

## Requirement coverage / traceability

| Requirement | Implementation | Test |
|---|---|---|
| **UX-CONTENT-016** section/field visibility | `ContentVisibilityEmbeds` section toggles + visible-field lists | e2e `content-visibility-and-embeds` (DM sees all; section set to dm-only drops from player data; persists across reload) |
| **UX-CONTENT-017** embeds reflect target + viewer permission | embeds list with object-card / note-section / render-block / unavailable states | e2e `content-visibility-and-embeds` (embedded target card; unavailable placeholder for hidden target) |

## Actor-safety / no-leak evidence

- The surface renders only the actor-filtered embed/visibility model: a player never gets a section
  toggle or a hidden field, and an embed the player cannot see resolves to one generic "unavailable"
  placeholder (never the target's name/fields). Seeding is DM-only (fail closed). The polish is
  presentation over that unchanged model.

## Tests / gates run

- `pnpm typecheck` — **0 errors, 0 warnings (4751 files)**.
- `pnpm lint` — **PASS**. `pnpm docs:validate` — **PASS** (via workpack).
- Visibility/embeds e2e, desktop + mobile — **10 pass**; axe gate `/knowledge` — **pass on both
  projects**.
- Full Playwright suite, BOTH projects — **see run below**.
- `pnpm ux-workpack:validate` — **PASS**.

## Files changed

Modified — GUI:
- `apps/gm/src/lib/gui/ContentVisibilityEmbeds.svelte` (token card/list/dl styling via scoped
  `:global`; testids preserved).

Generated by the UX workpack commands (do not hand-edit):
- `docs/planning/v2/ux/workpack-state.yaml`, `docs/planning/v2/ux/status.yaml`,
  `docs/planning/v2/ux/epics/UX-CONTENT-visibility-and-embeds.yaml`.

## Known gaps / deferred

- **Inline section-visibility markers in the editor (UX-CONTENT-016 §editor view):** section toggles
  live on this dedicated surface; surfacing per-section DM-only markers inline in the note editor
  rides with the three-pane editor shell.
- **Live embed transclusion in the editor/preview (UX-CONTENT-017):** embeds render as resolved cards
  on this surface; inline `![[ ]]` transclusion in the markdown preview is a follow-up.

## Git evidence

- Branch: `ux/UX-CONTENT-visibility-and-embeds` (off `e6b6709`).
- Commit: `feat(ux): UX-CONTENT granular visibility + embeds surface token polish`.

Final `git status --short` (pre-commit snapshot):

```
 M apps/gm/src/lib/gui/ContentVisibilityEmbeds.svelte
 M docs/planning/v2/ux/epics/UX-CONTENT-visibility-and-embeds.yaml
 M docs/planning/v2/ux/status.yaml
 M docs/planning/v2/ux/workpack-state.yaml
```
