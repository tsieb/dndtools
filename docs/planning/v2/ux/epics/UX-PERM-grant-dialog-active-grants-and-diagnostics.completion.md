# Completion — UX-PERM-grant-dialog-active-grants-and-diagnostics

UX workpack status: `complete`

Epic: Grant Dialog, Active Grants, and Diagnostics (phase "07 Collaboration and Permissions", P1).
Requirement coverage: `UX-PERM-002` (grant dialog), `UX-PERM-003` (active grants + revocation),
`UX-PERM-004` (permission status surface), `UX-PERM-005` (DM diagnostics panel).

## Summary

Token-styled the Settings collaboration/permission surfaces for visual cohesion, preserving every
functional testid via scoped `:global(...)` element selectors over the unchanged markup/logic.

- **`GrantManager`** (UX-PERM-002/003): the capability-set grant form and the active-grant list are
  now token cards — 44px controls, accent grant submit, raised grant rows with revoke actions.
- **`CapabilityStatus`** (UX-PERM-004): the platform-capability status list reads as toned rows
  (available = success tone, deferred/unsupported = warning tone).
- **`DiagnosticsPanel`** (UX-PERM-005): the DM health/sync/capability/schema sections + the support-
  bundle export become token cards with an error-toned unavailable state and a monospaced export
  preview; the DM-gating (denied state for non-DM) is unchanged.

## Requirement coverage / traceability

| Requirement | Implementation | Test |
|---|---|---|
| **UX-PERM-002/003** grant dialog + active grants + revoke | `GrantManager.svelte` | e2e `grant-management` (grant → appears → revoke) |
| **UX-PERM-004** permission/capability status surface | `CapabilityStatus.svelte` + `permission-roles` summary | e2e `permission-roles` (DM writable / observer read-only / player writable) |
| **UX-PERM-005** DM diagnostics panel (DM-gated) | `DiagnosticsPanel.svelte` | e2e `diagnostics` (health/sources/capabilities/schema, denied for non-DM, redacted bundle) |

## Actor-safety / no-leak evidence

- Diagnostics remain DM/grant-gated (`diagnostics-denied` for an unauthorized actor) and the support
  bundle defaults to redacted secrets; grants dispatch durable `permission.grant-capability-set` /
  revoke commands and render the actor-filtered grant state. The polish is presentation only.

## Tests / gates run

- `pnpm typecheck` — **0 errors, 0 warnings (4751 files)**.
- `pnpm lint` — **PASS**. `pnpm docs:validate` — **PASS** (via workpack).
- Grant / diagnostics / permission-roles e2e, desktop + mobile — **24 pass**; axe gate `/settings` +
  touch-target sweep `/settings` — **pass on both projects**.
- Full Playwright suite, BOTH projects — **see run below**.
- `pnpm ux-workpack:validate` — **PASS**.

## Files changed

Modified — GUI:
- `apps/gm/src/lib/gui/GrantManager.svelte`, `CapabilityStatus.svelte`, `DiagnosticsPanel.svelte`
  (token card/form styling via scoped `:global`; testids preserved).

Generated by the UX workpack commands (do not hand-edit):
- `docs/planning/v2/ux/workpack-state.yaml`, `docs/planning/v2/ux/status.yaml`,
  `docs/planning/v2/ux/epics/UX-PERM-grant-dialog-active-grants-and-diagnostics.yaml`.

## Known gaps / deferred

- **Stepped grant dialog (UX-PERM-002 §person → entity → set → preview → grant):** the grant flow is a
  single inline form with a capability preview rather than a multi-step modal wizard; the staged
  modal is a follow-up.
- **Dedicated player-facing "Your permissions" panel (UX-PERM-004):** capability/permission status is
  surfaced via `CapabilityStatus` + the permission-roles summary; a player-tailored read-only
  permissions card is a follow-up.

## Git evidence

- Branch: `ux/UX-PERM-grant-dialog-active-grants-and-diagnostics` (off `ef3c0b1`).
- Commit: `feat(ux): UX-PERM grant manager, capability status, diagnostics panel polish`.

Final `git status --short` (pre-commit snapshot):

```
 M apps/gm/src/lib/gui/CapabilityStatus.svelte
 M apps/gm/src/lib/gui/DiagnosticsPanel.svelte
 M apps/gm/src/lib/gui/GrantManager.svelte
 M docs/planning/v2/ux/epics/UX-PERM-grant-dialog-active-grants-and-diagnostics.yaml
 M docs/planning/v2/ux/status.yaml
 M docs/planning/v2/ux/workpack-state.yaml
```
