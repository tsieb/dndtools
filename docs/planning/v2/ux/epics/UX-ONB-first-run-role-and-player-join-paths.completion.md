# Completion — UX-ONB-first-run-role-and-player-join-paths

UX workpack status: `complete`

Epic: First-Run, Role, and Player Join Paths (phase "10 Onboarding and Production Polish", P1).
Requirement coverage: `UX-ONB-001` (first-run welcome), `UX-ONB-002` (role differentiation),
`UX-ONB-003` (setup steps), `UX-ONB-004` (feature-tier progressive disclosure), `UX-ONB-005`
(help surfaces), `UX-ONB-006` (read-only role parity).

## Summary

Gave the first-run onboarding surface (`FirstRun`) — the **first thing a user sees** — a proper sleek
treatment. It was previously unstyled (browser defaults); it now renders as a designed welcome on the
design tokens, preserving every functional `onboarding-*` / `feature-*` / `help-*` / `feature-tier-*`
testid.

- **UX-ONB-001/003 welcome + setup steps:** an accent-bordered welcome card with the vault-setup
  **checklist** (done steps get a filled-green check mark, pending an outline circle).
- **UX-ONB-002/006 role parity:** the DM sees the setup affordances; a non-DM sees the read-only
  guidance banner (the core `view.canSetup` gate is unchanged).
- **UX-ONB-004 feature-tier progressive disclosure:** the tier radiogroup renders as selectable
  **pills** (44px, capitalised, `:has(input:checked)` highlight) with the visible capabilities as chips.
- **UX-ONB-005 help surfaces:** the help disclosure is a clean token card with keyboard-reachable links
  to each surface.

## Requirement coverage / traceability

| Requirement | Implementation | Test |
|---|---|---|
| **UX-ONB-001/003** first-run welcome + setup steps + done state | `FirstRun.svelte` banner + checklist | e2e `onboarding` (first-run banner, steps, done state) |
| **UX-ONB-002/006** role differentiation + read-only parity | `view.canSetup` gate + read-only banner | e2e `onboarding` (DM setup vs read-only) |
| **UX-ONB-004** feature-tier progressive disclosure | tier radiogroup pills + visible features | e2e `onboarding` PLAT-013 (core/intermediate/advanced tiers reveal capabilities) |
| **UX-ONB-005** help surfaces | help disclosure | e2e `onboarding` (help surfaces reachable) |

## Actor-safety / no-leak evidence

- The surface renders only the core-computed `OnboardingView`; setup is DM-only (`view.canSetup` false
  → read-only guidance with no setup affordance), and the visible features reflect the active tier. No
  new state or read path; the polish is presentation over the unchanged model.

## Tests / gates run

- `pnpm typecheck` — **0 errors, 0 warnings (4755 files)**.
- `pnpm lint` — **PASS**. `pnpm docs:validate` — **PASS** (via workpack).
- Onboarding e2e (`onboarding`), desktop + mobile — **16 pass**; axe gate `/` + touch-target sweep `/`
  — **pass on both projects**.
- Full Playwright suite, BOTH projects — **see run below**.
- `pnpm ux-workpack:validate` — **PASS**.

## Files changed

Modified — GUI:
- `apps/gm/src/lib/gui/FirstRun.svelte` (add a token-styled onboarding treatment; testids preserved).

Generated by the UX workpack commands (do not hand-edit):
- `docs/planning/v2/ux/workpack-state.yaml`, `docs/planning/v2/ux/status.yaml`,
  `docs/planning/v2/ux/epics/UX-ONB-first-run-role-and-player-join-paths.yaml`.

## Known gaps / deferred

- **Dedicated player join-by-link/QR flow (UX-ONB-002/003 §spec):** role selection is the header
  `View as` switch + the core role floor; a stepped first-run role chooser and a player join-by-link/QR
  surface ride with the (deferred) live transport.

## Git evidence

- Branch: `ux/UX-ONB-first-run-role-and-player-join-paths` (off `cee63ea`).
- Commit: `feat(ux): UX-ONB first-run onboarding surface token treatment`.

Final `git status --short` (pre-commit snapshot):

```
 M apps/gm/src/lib/gui/FirstRun.svelte
 M docs/planning/v2/ux/epics/UX-ONB-first-run-role-and-player-join-paths.yaml
 M docs/planning/v2/ux/status.yaml
 M docs/planning/v2/ux/workpack-state.yaml
```
