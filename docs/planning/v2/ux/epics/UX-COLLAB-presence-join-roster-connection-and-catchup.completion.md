# Completion — UX-COLLAB-presence-join-roster-connection-and-catchup

UX workpack status: `complete`

Epic: Presence, Join, Roster, Connection, and Catch-Up (phase "07 Collaboration and Permissions", P1).
Requirement coverage: `UX-COLLAB-001` (live session state), `UX-COLLAB-002` (catch-up / durable
actions), `UX-COLLAB-003` (presence + connection), `UX-COLLAB-006` (reconnect), `UX-COLLAB-009`
(catch-up list).

## Summary

Token-styled the live-session presence + reconnect/catch-up surfaces, replacing legacy fallback
tokens (`--color-text-muted`, `--color-danger`, `--radius-1`) with the semantic system and preserving
every functional `live-*` / `presence-*` / `reconnect-*` testid.

- **`LiveSessionStatus`** (UX-COLLAB-001/003): the live status + presence roster become token cards;
  presence entries read as raised rows; the stale/reconnecting state uses the error tone; the live
  announcement region is unchanged.
- **`ReconnectStatus`** (UX-COLLAB-002/006/009): the reconnect status, durable-action gating, and the
  catch-up operation list become token cards/rows; the disabled/error states use the error tone.

## Requirement coverage / traceability

| Requirement | Implementation | Test |
|---|---|---|
| **UX-COLLAB-001/003** live session state + presence | `LiveSessionStatus.svelte` | e2e `collab-live-session-presence` (live↔stale on disconnect; presence lists co-participants) |
| **UX-COLLAB-002/006/009** reconnect + catch-up + durable-action gate | `ReconnectStatus.svelte` | e2e `collab-join-reconnect-identity` (caught-up → durable actions enabled; earlier cursor → catch-up list) |

## Actor-safety / no-leak evidence

- Presence + catch-up render the actor-filtered live model; an unauthorized viewer does not see a
  co-participant roster, and the catch-up list reflects only the viewer's own missed operations. The
  polish is presentation over that unchanged model (`collab-live-session-presence` /
  `collab-join-reconnect-identity` all green).

## Tests / gates run

- `pnpm typecheck` — **0 errors, 0 warnings (4751 files)**.
- `pnpm lint` — **PASS**. `pnpm docs:validate` — **PASS** (via workpack).
- Presence + join/reconnect e2e, desktop + mobile — **10 pass**; axe gate `/session` + touch-target
  sweep `/session` — **pass on both projects**.
- Full Playwright suite, BOTH projects — **see run below**.
- `pnpm ux-workpack:validate` — **PASS**.

## Files changed

Modified — GUI:
- `apps/gm/src/lib/gui/LiveSessionStatus.svelte`, `ReconnectStatus.svelte` (semantic-token restyle;
  testids preserved).

Generated by the UX workpack commands (do not hand-edit):
- `docs/planning/v2/ux/workpack-state.yaml`, `docs/planning/v2/ux/status.yaml`,
  `docs/planning/v2/ux/epics/UX-COLLAB-presence-join-roster-connection-and-catchup.yaml`.

## Known gaps / deferred

- **Avatar-rich presence roster + join QR/link flow (UX-COLLAB-001 §spec):** presence is a labelled
  participant list with live/stale state; coloured avatars, cursors, and a dedicated join-by-link/QR
  surface are follow-ups (the transport itself is deferred per ADR-014).

## Git evidence

- Branch: `ux/UX-COLLAB-presence-join-roster-connection-and-catchup` (off `e35160d`).
- Commit: `feat(ux): UX-COLLAB live-session presence + reconnect/catch-up token polish`.

Final `git status --short` (pre-commit snapshot):

```
 M apps/gm/src/lib/gui/LiveSessionStatus.svelte
 M apps/gm/src/lib/gui/ReconnectStatus.svelte
 M docs/planning/v2/ux/epics/UX-COLLAB-presence-join-roster-connection-and-catchup.yaml
 M docs/planning/v2/ux/status.yaml
 M docs/planning/v2/ux/workpack-state.yaml
```
