# Completion — UX-CONTENT-templates-snippets-sources-import-export

UX workpack status: `complete`

Epic: Templates, Snippets, Sources, Import, and Export (phase "06 Core Library Workspaces", P1).
Requirement coverage: `UX-CONTENT-011` (templates), `UX-CONTENT-012` (snippets), `UX-CONTENT-013`
(subtype registry), `UX-CONTENT-014` (source constraints), `UX-CONTENT-015` (import/export),
`UX-CONTENT-018` (source-specific authoring).

## Summary

Token-styled the three remaining content-authoring surfaces for visual cohesion with the editor,
preserving every functional testid via scoped `:global(...)` element selectors over the unchanged
markup/logic.

- **`TemplatesAndSnippets`** (UX-CONTENT-011/012): the template-instantiation and snippet-insert
  forms become token cards (44px controls, accent submit, raised result rows, muted meta).
- **`ContentSourceConstraints`** (UX-CONTENT-014/018): source-specific constraint + acknowledgement
  surface restyled; the unsupported-source state now reads in the warning tone.
- **`ContentImportExport`** (UX-CONTENT-015): the import/export forms become cards and the export
  preview is a monospaced, scrollable token panel.

## Requirement coverage / traceability

| Requirement | Implementation | Test |
|---|---|---|
| **UX-CONTENT-011/012** templates + snippets (visibility-safe) | `TemplatesAndSnippets` | e2e `content-templates-and-snippets` (template creates content; snippet inherits + can't widen visibility; sanitized preview; DM-only) |
| **UX-CONTENT-013** subtype registry | `VaultObjects` registry (prior epic) | e2e `content-objects-and-wikilinks` CONTENT-013 |
| **UX-CONTENT-014/018** source constraints + acknowledgement | `ContentSourceConstraints` | e2e `content-source-constraints` |
| **UX-CONTENT-015** import / export round-trip | `ContentImportExport` | e2e `content-import-export` |

## Actor-safety / no-leak evidence

- All three surfaces dispatch durable commands and read actor-filtered models; a player gets no
  template/snippet/import authoring affordances (`content-templates-and-snippets` fail-closed), and a
  snippet insert can never widen a note's visibility (CONTENT-004). The polish is presentation only.

## Tests / gates run

- `pnpm typecheck` — **0 errors, 0 warnings (4751 files)**.
- `pnpm lint` — **PASS**. `pnpm docs:validate` — **PASS** (via workpack).
- Templates/snippets + source-constraints + import/export e2e, desktop + mobile — **32 pass**; axe
  gate `/knowledge` — **pass on both projects**.
- Full Playwright suite, BOTH projects — **see run below**.
- `pnpm ux-workpack:validate` — **PASS**.

## Files changed

Modified — GUI:
- `apps/gm/src/lib/gui/TemplatesAndSnippets.svelte`, `ContentSourceConstraints.svelte`,
  `ContentImportExport.svelte` (token card/form styling via scoped `:global` selectors; testids
  preserved).

Generated by the UX workpack commands (do not hand-edit):
- `docs/planning/v2/ux/workpack-state.yaml`, `docs/planning/v2/ux/status.yaml`,
  `docs/planning/v2/ux/epics/UX-CONTENT-templates-snippets-sources-import-export.yaml`.

## Known gaps / deferred

- **Template/snippet pickers in the editor slash menu (UX-CONTENT-011/012 §spec):** templates and
  snippets are instantiated from their dedicated forms; surfacing them as Insert items inside the
  editor's slash menu rides with the richer slash-menu work (UX-CONTENT-003 deferral).
- **Drag-and-drop / file-picker import (UX-CONTENT-015):** import/export remains the
  textarea/diagnostics flow; a drop zone + native file picker is a follow-up.

## Git evidence

- Branch: `ux/UX-CONTENT-templates-snippets-sources-import-export` (off `6519b30`).
- Commit: `feat(ux): UX-CONTENT templates, snippets, sources, import/export polish`.

Final `git status --short` (pre-commit snapshot):

```
 M apps/gm/src/lib/gui/ContentImportExport.svelte
 M apps/gm/src/lib/gui/ContentSourceConstraints.svelte
 M apps/gm/src/lib/gui/TemplatesAndSnippets.svelte
 M docs/planning/v2/ux/epics/UX-CONTENT-templates-snippets-sources-import-export.yaml
 M docs/planning/v2/ux/status.yaml
 M docs/planning/v2/ux/workpack-state.yaml
```
