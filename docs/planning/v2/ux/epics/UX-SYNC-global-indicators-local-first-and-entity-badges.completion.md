# Completion — UX-SYNC-global-indicators-local-first-and-entity-badges

UX workpack status: `complete`

Epic: Sync Indicators, Local-First Feedback, and Entity Badges (phase "09 Reliability and Optional
Capabilities", P1). Requirement coverage: `UX-SYNC-001` (global indicators), `UX-SYNC-002`/`003`
(local-first feedback), `UX-SYNC-008`, `UX-SYNC-011`/`013` (entity/lineage badges).

## Summary

Token-polished the sync status / lineage / conflict surface (`SyncStatusPanel`) — the health/online
indicator, source health, pending-changes-on-this-device list, inbound revisions, conflicts, and the
conflict-lifecycle resolution surface become consistent token cards with an error-toned
unavailable/unresolved state and 44px controls. Every functional `sync-*` / `conflict-*` testid is
preserved.

## Requirement coverage / traceability

| Requirement | Implementation | Test |
|---|---|---|
| **UX-SYNC-001** global health/online + pending indicator | `SyncStatusPanel` header | e2e `sync-status-and-lineage` (health/online/pending) |
| **UX-SYNC-002/003/008** local-first pending changes + source health | pending-outbound + sources lists | e2e `sync-local-first` (offline workflows); `sync-source-adapters` |
| **UX-SYNC-011/013** lineage + per-entity conflict badges (actor-filtered) | inbound revisions + conflict + conflict-lifecycle | e2e `sync-status-and-lineage` SYNC-014 (DM lineage; player/observer non-leaking freshness only); `sync-conflict-lifecycle` |

## Actor-safety / no-leak evidence

- The panel reads the actor-filtered sync model: a player/observer sees only a non-leaking freshness
  summary (never the DM's structural version history/lineage or diverging conflict values), and
  conflict resolution is the DM-authorized command (fail closed) — `sync-status-and-lineage` SYNC-014
  + `sync-conflict-lifecycle` all green. The polish is presentation over the unchanged model.

## Tests / gates run

- `pnpm typecheck` — **0 errors, 0 warnings (4753 files)**.
- `pnpm lint` — **PASS**. `pnpm docs:validate` — **PASS** (via workpack).
- Sync e2e (`sync-status-and-lineage` + `sync-conflict-lifecycle` + `sync-local-first`), desktop +
  mobile — **24 pass**; axe gate `/settings` + touch-target sweep `/settings` — **pass on both
  projects**.
- Full Playwright suite, BOTH projects — **see run below**.
- `pnpm ux-workpack:validate` — **PASS**.

## Files changed

Modified — GUI:
- `apps/gm/src/lib/gui/SyncStatusPanel.svelte` (token card/list/badge polish; testids preserved).

Generated by the UX workpack commands (do not hand-edit):
- `docs/planning/v2/ux/workpack-state.yaml`, `docs/planning/v2/ux/status.yaml`,
  `docs/planning/v2/ux/epics/UX-SYNC-global-indicators-local-first-and-entity-badges.yaml`.

## Known gaps / deferred

- **Per-entity inline sync badges on entity rows (UX-SYNC-013 §spec):** sync state is surfaced on the
  central status panel; threading a small per-entity freshness/conflict badge onto every entity row
  across surfaces is a cross-cutting follow-up.

## Git evidence

- Branch: `ux/UX-SYNC-global-indicators-local-first-and-entity-badges` (off `6153bfd`).
- Commit: `feat(ux): UX-SYNC status/lineage/conflict panel token polish`.

Final `git status --short` (pre-commit snapshot):

```
 M apps/gm/src/lib/gui/SyncStatusPanel.svelte
 M docs/planning/v2/ux/epics/UX-SYNC-global-indicators-local-first-and-entity-badges.yaml
 M docs/planning/v2/ux/status.yaml
 M docs/planning/v2/ux/workpack-state.yaml
```
