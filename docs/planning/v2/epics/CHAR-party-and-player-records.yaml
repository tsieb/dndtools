schemaVersion: 1
id: CHAR-party-and-player-records
title: "CHAR: Party and player records"
status: complete
approved: true
domain: CHAR
capabilityBranch: Party and player records
objective: Deliver the party and player records capability branch for CHAR,
  traced to the included v2 requirements.
requirementIds:
  - CHAR-011
  - CHAR-012
  - CHAR-015
  - CHAR-016
excludedRequirementIds: []
architectureContracts:
  - "Contract 1: Processing / Display Decoupling"
  - "Contract 2: Cloud Sync & Offline Model"
  - "Contract 3: Role, Visibility & Permission Grant Model"
  - "Contract 4: Scene and Widget Contract"
expectedAffectedAreas:
  - apps/gm
  - docs/planning/v2
  - character model
  - character UI
dependencies: []
parallelSafety:
  fileOwnership:
    - apps/gm/char/
    - docs/planning/v2/
  requiresInterfaceContract: true
  notes: Parallel execution requires explicit interface contracts and
    non-overlapping generated file ownership.
stories:
  - id: CHAR-011-S01
    title: The DM and players shall be able to view a party overview with visible
      HP/status/resource summaries, marching order, and party inventory
      according to visibility and grants
    requirementIds:
      - CHAR-011
    acceptanceCriteria:
      - Given three visible PCs, when the party overview opens, then visible
        combat summaries and marching order are shown.
      - Given one character is not visible to a participant, when the party
        overview loads, then that character is omitted.
    tasks:
      - id: CHAR-011-S01-T01
        title: Confirm architecture and interface shape for CHAR-011
        kind: design
      - id: CHAR-011-S01-T02
        title: Implement the smallest behavior that satisfies CHAR-011
        kind: implementation
      - id: CHAR-011-S01-T03
        title: Add automated coverage for CHAR-011 acceptance criteria
        kind: test
      - id: CHAR-011-S01-T04
        title: Record demo notes and traceability evidence for CHAR-011
        kind: demo
  - id: CHAR-012-S02
    title: A player shall be able to keep a character journal, bookmarks, NPC
      impressions, personal quests, and session highlights scoped to their
      character permissions
    requirementIds:
      - CHAR-012
    acceptanceCriteria:
      - Given a player owns a character, when they add a private journal entry,
        then the entry is visible to the player and DM according to the
        permission model.
      - Given a player without ownership attempts to edit the journal, when they
        submit content, then the command is rejected.
      - Given a journal entry is `shared` only through the owning character's
        `owner` grant, when other players, observers, search, graph, widgets, or
        MCP responses are generated, then the entry content and revealing
        metadata are absent.
    tasks:
      - id: CHAR-012-S02-T01
        title: Confirm architecture and interface shape for CHAR-012
        kind: design
      - id: CHAR-012-S02-T02
        title: Implement the smallest behavior that satisfies CHAR-012
        kind: implementation
      - id: CHAR-012-S02-T03
        title: Add automated coverage for CHAR-012 acceptance criteria
        kind: test
      - id: CHAR-012-S02-T04
        title: Record demo notes and traceability evidence for CHAR-012
        kind: demo
  - id: CHAR-015-S03
    title: Observer character access shall be denied by default, including party
      overviews, combat detail panels, search results, widgets, and MCP
      responses, unless a specific visible non-character projection is delivered
    requirementIds:
      - CHAR-015
    acceptanceCriteria:
      - Given an observer views shared combat, when character data is requested,
        then no character sheet, private resource, or owner journal data is
        returned.
      - Given a DM projects a visible stat-block summary to observers, when it
        renders, then only the explicitly projected summary fields are
        delivered.
    tasks:
      - id: CHAR-015-S03-T01
        title: Confirm architecture and interface shape for CHAR-015
        kind: design
      - id: CHAR-015-S03-T02
        title: Implement the smallest behavior that satisfies CHAR-015
        kind: implementation
      - id: CHAR-015-S03-T03
        title: Add automated coverage for CHAR-015 acceptance criteria
        kind: test
      - id: CHAR-015-S03-T04
        title: Record demo notes and traceability evidence for CHAR-015
        kind: demo
  - id: CHAR-016-S04
    title: Character journal visibility shall be explicit per entry using canonical
      visibility states and ownership grants, with DM access, owner access,
      other-player filtering, and cross-surface invalidation enforced by the
      data layer
    requirementIds:
      - CHAR-016
    acceptanceCriteria:
      - Given a player creates a journal entry, when no visibility is selected,
        then the entry defaults to `shared` for the owning character's `owner`
        grant and remains DM-auditable.
      - Given the DM changes journal visibility, when sync streams update, then
        affected player caches, search results, graph edges, and widgets are
        invalidated before new content is delivered.
      - Given a journal entry is `shared` with Player A through a viewer-capable
        grant, when Player A and Player B query journals, then only Player A
        receives the entry.
      - Given a journal entry is visible only to the owner, when another player,
        observer, widget, search, graph, or MCP surface requests it, then no
        title, snippet, id, count, or relationship edge is returned.
    tasks:
      - id: CHAR-016-S04-T01
        title: Confirm architecture and interface shape for CHAR-016
        kind: design
      - id: CHAR-016-S04-T02
        title: Implement the smallest behavior that satisfies CHAR-016
        kind: implementation
      - id: CHAR-016-S04-T03
        title: Add automated coverage for CHAR-016 acceptance criteria
        kind: test
      - id: CHAR-016-S04-T04
        title: Record demo notes and traceability evidence for CHAR-016
        kind: demo
qualityBar:
  - "Correctness: every mapped acceptance criterion is implemented or explicitly
    deferred with reviewer-visible evidence."
  - "Architecture integrity: changes obey ADR-014, the architecture contracts,
    package boundaries, and no-v1-runtime-import rules."
  - "Traceability: requirement IDs are tied to code, tests, demo notes, and any
    documentation changes."
  - "Test depth: unit, integration, e2e, boundary, accessibility, performance,
    security, permission, sync, and migration coverage are chosen according to
    risk."
  - "User experience: visible flows are complete, accessible, responsive,
    resilient to empty/error/loading states, and consistent with the design
    system."
  - "Data safety: persistence, offline behavior, sync assumptions, conflict
    handling, actor filtering, and privacy boundaries fail closed."
  - "Maintainability: modules remain cohesive, typed, readable, locally scoped,
    and free of speculative abstractions or unrelated refactors."
  - "Operational quality: diagnostics, metrics, docs, generated files, and
    handoff evidence are current before completion."
gitWorkflow:
  - Start from a clean working tree. Run `git status --short` before editing and
    stop if unrelated changes overlap this epic.
  - Use one branch per epic from the correct base branch, and keep all commits
    scoped to the assigned epic.
  - Never reset, overwrite, or reformat unrelated user changes. Work around
    existing dirty files unless they block the epic.
  - Commit the code, docs, tests, generated workpack updates, and completion
    evidence needed for the epic before handoff.
  - "Leave a clean slate when the epic is complete: no untracked files, no
    unstaged edits, and no stale generated planning diffs caused by the epic."
statusAutomation:
  - Do not hand-edit generated epic packets, `status.yaml`,
    `requirements-index.yaml`, `initiative-map.yaml`, or `parallel-batches.yaml`
    for status changes.
  - Use `pnpm workpack:set-status -- --epic CHAR-party-and-player-records
    --status active` when implementation starts.
  - Create `docs/planning/v2/epics/CHAR-party-and-player-records.completion.md`
    with demo, tests, traceability, gaps, and git evidence before marking
    complete.
  - Use `pnpm workpack:complete -- --epic CHAR-party-and-player-records` to
    update `workpack-state.yaml` and regenerate all derived planning files
    together.
  - Run `pnpm workpack:validate` after any status or generation command;
    validation fails if generated files drift from the source of truth.
testPlan:
  - Run unit tests for domain reducers, command validators, and data transforms
    touched by this epic.
  - Run UI or integration checks for any visible prototype behavior touched by
    this epic.
  - Run domain-specific quality gates for accessibility, performance, security,
    permissions, sync/offline behavior, migrations, and docs when those areas
    are touched.
  - Run `pnpm workpack:validate` before handoff.
demoNotesTemplate:
  - Describe the user-visible path demonstrated.
  - List the requirement IDs exercised by the demo.
  - Record any gaps intentionally deferred out of this epic.
stopConditions:
  - Stop if the v2 stack ADR is missing or contradicts the implementation
    approach.
  - Stop if implementing the epic requires runtime imports from v1 app code.
  - Stop if hidden visibility, permission, sync, or persistence behavior is
    ambiguous.
  - Stop if the generated workpack no longer validates.
  - Stop if `git status --short` shows unrelated changes that overlap the files
    this epic needs.
completionEvidence:
  - Targeted tests pass.
  - Traceability from requirement IDs to implementation and tests is documented.
  - Demo notes are filled in for visible behavior.
  - Quality review covers correctness, architecture, security, permissions,
    accessibility, performance, persistence, sync/offline assumptions,
    maintainability, UX polish, and documentation.
  - Completion evidence file records demo path, tests run, changed files,
    requirement coverage, known gaps, git branch, commit or PR, and final `git
    status --short` output.
  - Workpack status is updated with the programmatic complete command after
    completion evidence exists.
  - The epic leaves a clean git slate with no untracked or unstaged files caused
    by the work.
completionEvidenceFile: docs/planning/v2/epics/CHAR-party-and-player-records.completion.md
