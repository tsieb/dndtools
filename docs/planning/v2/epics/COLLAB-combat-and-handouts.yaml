schemaVersion: 1
id: COLLAB-combat-and-handouts
title: "COLLAB: Combat and handouts"
status: complete
approved: true
domain: COLLAB
capabilityBranch: Combat and handouts
objective: Deliver the combat and handouts capability branch for COLLAB, traced
  to the included v2 requirements.
requirementIds:
  - COLLAB-006
  - COLLAB-007
  - COLLAB-012
excludedRequirementIds: []
architectureContracts:
  - "Contract 2: Cloud Sync & Offline Model"
  - "Contract 3: Role, Visibility & Permission Grant Model"
  - "Contract 4: Scene and Widget Contract"
expectedAffectedAreas:
  - apps/v2
  - docs/planning/v2
  - collaboration transport
  - participant state
dependencies: []
parallelSafety:
  fileOwnership:
    - apps/v2/collab/
    - docs/planning/v2/
  requiresInterfaceContract: true
  notes: Parallel execution requires explicit interface contracts and
    non-overlapping generated file ownership.
stories:
  - id: COLLAB-006-S01
    title: Participants shall be able to view shared combat state according to role
      and grants, including current turn, visible combatants, HP/status
      summaries, and permitted interaction controls
    requirementIds:
      - COLLAB-006
    acceptanceCriteria:
      - Given a player can see their character in combat, when combat state
        updates, then visible turn and status data refresh.
      - Given a hidden enemy is in initiative, when a player views combat, then
        the hidden combatant is omitted or represented only by DM-approved
        placeholder.
      - Given cached session combat is available offline, when remote delivery
        is unavailable, then the participant can view cached visible state
        marked stale and cannot submit commands requiring live authority.
    tasks:
      - id: COLLAB-006-S01-T01
        title: Confirm architecture and interface shape for COLLAB-006
        kind: design
      - id: COLLAB-006-S01-T02
        title: Implement the smallest behavior that satisfies COLLAB-006
        kind: implementation
      - id: COLLAB-006-S01-T03
        title: Add automated coverage for COLLAB-006 acceptance criteria
        kind: test
      - id: COLLAB-006-S01-T04
        title: Record demo notes and traceability evidence for COLLAB-006
        kind: demo
  - id: COLLAB-007-S02
    title: The DM shall be able to deliver handouts, images, notes, map fragments,
      ciphers, and rumors to selected players with delivery acknowledgement and
      revocation state
    requirementIds:
      - COLLAB-007
    acceptanceCriteria:
      - Given a player is online, when a handout is delivered, then the DM sees
        delivered/opened status when supported.
      - Given the DM revokes a handout, when the player next syncs, then the
        handout widget is removed unless persistent access was explicitly
        granted.
      - Given delivery and revocation operations are both queued while the
        player is offline, when the player reconnects, then replay order and
        final visibility determine whether the handout is delivered, removed, or
        retained by persistent grant.
    tasks:
      - id: COLLAB-007-S02-T01
        title: Confirm architecture and interface shape for COLLAB-007
        kind: design
      - id: COLLAB-007-S02-T02
        title: Implement the smallest behavior that satisfies COLLAB-007
        kind: implementation
      - id: COLLAB-007-S02-T03
        title: Add automated coverage for COLLAB-007 acceptance criteria
        kind: test
      - id: COLLAB-007-S02-T04
        title: Record demo notes and traceability evidence for COLLAB-007
        kind: demo
  - id: COLLAB-012-S03
    title: The DM shall be able to create Player Groups for projection and handout
      delivery targets, with group membership changes affecting delivery only
      and not granting visibility or write permissions by themselves
    requirementIds:
      - COLLAB-012
    acceptanceCriteria:
      - Given a handout is delivered to a Player Group, when membership is
        evaluated, then only current group members receive the delivery
        operation.
      - Given a player is added to a Player Group after a prior handout
        delivery, when no persistent access grant exists, then the prior handout
        is not automatically delivered.
      - Given the DM edits Player Groups offline, when remote delivery resumes,
        then group membership changes are applied before later queued deliveries
        that depend on them.
    tasks:
      - id: COLLAB-012-S03-T01
        title: Confirm architecture and interface shape for COLLAB-012
        kind: design
      - id: COLLAB-012-S03-T02
        title: Implement the smallest behavior that satisfies COLLAB-012
        kind: implementation
      - id: COLLAB-012-S03-T03
        title: Add automated coverage for COLLAB-012 acceptance criteria
        kind: test
      - id: COLLAB-012-S03-T04
        title: Record demo notes and traceability evidence for COLLAB-012
        kind: demo
qualityBar:
  - "Correctness: every mapped acceptance criterion is implemented or explicitly
    deferred with reviewer-visible evidence."
  - "Architecture integrity: changes obey ADR-014, the architecture contracts,
    package boundaries, and no-v1-runtime-import rules."
  - "Traceability: requirement IDs are tied to code, tests, demo notes, and any
    documentation changes."
  - "Test depth: unit, integration, e2e, boundary, accessibility, performance,
    security, permission, sync, and migration coverage are chosen according to
    risk."
  - "User experience: visible flows are complete, accessible, responsive,
    resilient to empty/error/loading states, and consistent with the design
    system."
  - "Data safety: persistence, offline behavior, sync assumptions, conflict
    handling, actor filtering, and privacy boundaries fail closed."
  - "Maintainability: modules remain cohesive, typed, readable, locally scoped,
    and free of speculative abstractions or unrelated refactors."
  - "Operational quality: diagnostics, metrics, docs, generated files, and
    handoff evidence are current before completion."
gitWorkflow:
  - Start from a clean working tree. Run `git status --short` before editing and
    stop if unrelated changes overlap this epic.
  - Use one branch per epic from the correct base branch, and keep all commits
    scoped to the assigned epic.
  - Never reset, overwrite, or reformat unrelated user changes. Work around
    existing dirty files unless they block the epic.
  - Commit the code, docs, tests, generated workpack updates, and completion
    evidence needed for the epic before handoff.
  - "Leave a clean slate when the epic is complete: no untracked files, no
    unstaged edits, and no stale generated planning diffs caused by the epic."
statusAutomation:
  - Do not hand-edit generated epic packets, `status.yaml`,
    `requirements-index.yaml`, `initiative-map.yaml`, or `parallel-batches.yaml`
    for status changes.
  - Use `pnpm v2:workpack:set-status -- --epic COLLAB-combat-and-handouts
    --status active` when implementation starts.
  - Create `docs/planning/v2/epics/COLLAB-combat-and-handouts.completion.md`
    with demo, tests, traceability, gaps, and git evidence before marking
    complete.
  - Use `pnpm v2:workpack:complete -- --epic COLLAB-combat-and-handouts` to
    update `workpack-state.yaml` and regenerate all derived planning files
    together.
  - Run `pnpm v2:workpack:validate` after any status or generation command;
    validation fails if generated files drift from the source of truth.
testPlan:
  - Run unit tests for domain reducers, command validators, and data transforms
    touched by this epic.
  - Run UI or integration checks for any visible prototype behavior touched by
    this epic.
  - Run domain-specific quality gates for accessibility, performance, security,
    permissions, sync/offline behavior, migrations, and docs when those areas
    are touched.
  - Run `pnpm v2:workpack:validate` before handoff.
demoNotesTemplate:
  - Describe the user-visible path demonstrated.
  - List the requirement IDs exercised by the demo.
  - Record any gaps intentionally deferred out of this epic.
stopConditions:
  - Stop if the v2 stack ADR is missing or contradicts the implementation
    approach.
  - Stop if implementing the epic requires runtime imports from v1 app code.
  - Stop if hidden visibility, permission, sync, or persistence behavior is
    ambiguous.
  - Stop if the generated workpack no longer validates.
  - Stop if `git status --short` shows unrelated changes that overlap the files
    this epic needs.
completionEvidence:
  - Targeted tests pass.
  - Traceability from requirement IDs to implementation and tests is documented.
  - Demo notes are filled in for visible behavior.
  - Quality review covers correctness, architecture, security, permissions,
    accessibility, performance, persistence, sync/offline assumptions,
    maintainability, UX polish, and documentation.
  - Completion evidence file records demo path, tests run, changed files,
    requirement coverage, known gaps, git branch, commit or PR, and final `git
    status --short` output.
  - Workpack status is updated with the programmatic complete command after
    completion evidence exists.
  - The epic leaves a clean git slate with no untracked or unstaged files caused
    by the work.
completionEvidenceFile: docs/planning/v2/epics/COLLAB-combat-and-handouts.completion.md
