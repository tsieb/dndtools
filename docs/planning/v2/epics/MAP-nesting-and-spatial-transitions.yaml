schemaVersion: 1
id: MAP-nesting-and-spatial-transitions
title: "MAP: Nesting and spatial transitions"
status: complete
approved: true
domain: MAP
capabilityBranch: Nesting and spatial transitions
objective: Deliver the nesting and spatial transitions capability branch for
  MAP, traced to the included v2 requirements.
requirementIds:
  - MAP-008
  - MAP-009
  - MAP-017
excludedRequirementIds: []
architectureContracts:
  - "Contract 3: Role, Visibility & Permission Grant Model"
expectedAffectedAreas:
  - apps/v2
  - docs/planning/v2
  - map model
  - map renderer
dependencies: []
parallelSafety:
  fileOwnership:
    - apps/v2/map/
    - docs/planning/v2/
  requiresInterfaceContract: true
  notes: Parallel execution requires explicit interface contracts and
    non-overlapping generated file ownership.
stories:
  - id: MAP-008-S01
    title: The DM shall be able to embed one map inside another at configured
      position, scale, rotation, and transition behavior while preserving each
      map's independent layers and permissions
    requirementIds:
      - MAP-008
    acceptanceCriteria:
      - Given a city map nested inside a region map, when the DM opens the
        region map, then the nested map appears at its configured spatial
        transform.
      - Given the city map has DM-only layers, when a player zooms into the
        nested map, then those layers remain unavailable.
    tasks:
      - id: MAP-008-S01-T01
        title: Confirm architecture and interface shape for MAP-008
        kind: design
      - id: MAP-008-S01-T02
        title: Implement the smallest behavior that satisfies MAP-008
        kind: implementation
      - id: MAP-008-S01-T03
        title: Add automated coverage for MAP-008 acceptance criteria
        kind: test
      - id: MAP-008-S01-T04
        title: Record demo notes and traceability evidence for MAP-008
        kind: demo
  - id: MAP-009-S02
    title: A participant viewing a nested map shall be able to smoothly scroll or
      zoom between parent and child maps within the limits of their visible map
      data
    requirementIds:
      - MAP-009
    acceptanceCriteria:
      - Given a visible nested map relationship, when the user zooms past the
        configured threshold, then the viewport transitions to the child map.
      - Given the child map is not visible to the participant, when the
        threshold is reached, then the transition is blocked without revealing
        the child map name or content unless separately visible.
    tasks:
      - id: MAP-009-S02-T01
        title: Confirm architecture and interface shape for MAP-009
        kind: design
      - id: MAP-009-S02-T02
        title: Implement the smallest behavior that satisfies MAP-009
        kind: implementation
      - id: MAP-009-S02-T03
        title: Add automated coverage for MAP-009 acceptance criteria
        kind: test
      - id: MAP-009-S02-T04
        title: Record demo notes and traceability evidence for MAP-009
        kind: demo
  - id: MAP-017-S03
    title: Nested map relationships shall prevent cycles, preserve coordinate
      transforms across the configured supported depth, and surface broken
      child-map links without leaking hidden child names or content
    requirementIds:
      - MAP-017
    acceptanceCriteria:
      - Given a map is already an ancestor of a candidate child, when the DM
        attempts to nest the ancestor under the child, then the command is
        rejected as a cycle.
      - Given a user transitions world to region to city to building, when every
        child is visible, then coordinates and scale transforms resolve
        deterministically at each level.
      - Given a child map is deleted or hidden from the participant, when the
        parent renders, then the nested area shows a missing or unavailable
        state without revealing hidden metadata.
    tasks:
      - id: MAP-017-S03-T01
        title: Confirm architecture and interface shape for MAP-017
        kind: design
      - id: MAP-017-S03-T02
        title: Implement the smallest behavior that satisfies MAP-017
        kind: implementation
      - id: MAP-017-S03-T03
        title: Add automated coverage for MAP-017 acceptance criteria
        kind: test
      - id: MAP-017-S03-T04
        title: Record demo notes and traceability evidence for MAP-017
        kind: demo
qualityBar:
  - "Correctness: every mapped acceptance criterion is implemented or explicitly
    deferred with reviewer-visible evidence."
  - "Architecture integrity: changes obey ADR-014, the architecture contracts,
    package boundaries, and no-v1-runtime-import rules."
  - "Traceability: requirement IDs are tied to code, tests, demo notes, and any
    documentation changes."
  - "Test depth: unit, integration, e2e, boundary, accessibility, performance,
    security, permission, sync, and migration coverage are chosen according to
    risk."
  - "User experience: visible flows are complete, accessible, responsive,
    resilient to empty/error/loading states, and consistent with the design
    system."
  - "Data safety: persistence, offline behavior, sync assumptions, conflict
    handling, actor filtering, and privacy boundaries fail closed."
  - "Maintainability: modules remain cohesive, typed, readable, locally scoped,
    and free of speculative abstractions or unrelated refactors."
  - "Operational quality: diagnostics, metrics, docs, generated files, and
    handoff evidence are current before completion."
gitWorkflow:
  - Start from a clean working tree. Run `git status --short` before editing and
    stop if unrelated changes overlap this epic.
  - Use one branch per epic from the correct base branch, and keep all commits
    scoped to the assigned epic.
  - Never reset, overwrite, or reformat unrelated user changes. Work around
    existing dirty files unless they block the epic.
  - Commit the code, docs, tests, generated workpack updates, and completion
    evidence needed for the epic before handoff.
  - "Leave a clean slate when the epic is complete: no untracked files, no
    unstaged edits, and no stale generated planning diffs caused by the epic."
statusAutomation:
  - Do not hand-edit generated epic packets, `status.yaml`,
    `requirements-index.yaml`, `initiative-map.yaml`, or `parallel-batches.yaml`
    for status changes.
  - Use `pnpm v2:workpack:set-status -- --epic
    MAP-nesting-and-spatial-transitions --status active` when implementation
    starts.
  - Create
    `docs/planning/v2/epics/MAP-nesting-and-spatial-transitions.completion.md`
    with demo, tests, traceability, gaps, and git evidence before marking
    complete.
  - Use `pnpm v2:workpack:complete -- --epic
    MAP-nesting-and-spatial-transitions` to update `workpack-state.yaml` and
    regenerate all derived planning files together.
  - Run `pnpm v2:workpack:validate` after any status or generation command;
    validation fails if generated files drift from the source of truth.
testPlan:
  - Run unit tests for domain reducers, command validators, and data transforms
    touched by this epic.
  - Run UI or integration checks for any visible prototype behavior touched by
    this epic.
  - Run domain-specific quality gates for accessibility, performance, security,
    permissions, sync/offline behavior, migrations, and docs when those areas
    are touched.
  - Run `pnpm v2:workpack:validate` before handoff.
demoNotesTemplate:
  - Describe the user-visible path demonstrated.
  - List the requirement IDs exercised by the demo.
  - Record any gaps intentionally deferred out of this epic.
stopConditions:
  - Stop if the v2 stack ADR is missing or contradicts the implementation
    approach.
  - Stop if implementing the epic requires runtime imports from v1 app code.
  - Stop if hidden visibility, permission, sync, or persistence behavior is
    ambiguous.
  - Stop if the generated workpack no longer validates.
  - Stop if `git status --short` shows unrelated changes that overlap the files
    this epic needs.
completionEvidence:
  - Targeted tests pass.
  - Traceability from requirement IDs to implementation and tests is documented.
  - Demo notes are filled in for visible behavior.
  - Quality review covers correctness, architecture, security, permissions,
    accessibility, performance, persistence, sync/offline assumptions,
    maintainability, UX polish, and documentation.
  - Completion evidence file records demo path, tests run, changed files,
    requirement coverage, known gaps, git branch, commit or PR, and final `git
    status --short` output.
  - Workpack status is updated with the programmatic complete command after
    completion evidence exists.
  - The epic leaves a clean git slate with no untracked or unstaged files caused
    by the work.
completionEvidenceFile: docs/planning/v2/epics/MAP-nesting-and-spatial-transitions.completion.md
