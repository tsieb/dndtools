schemaVersion: 1
id: CONTENT-notes-and-editor
title: "CONTENT: Notes and editor"
status: complete
approved: true
domain: CONTENT
capabilityBranch: Notes and editor
objective: Deliver the notes and editor capability branch for CONTENT, traced to
  the included v2 requirements.
requirementIds:
  - CONTENT-001
  - CONTENT-002
excludedRequirementIds: []
architectureContracts:
  - "Contract 4: Scene and Widget Contract"
expectedAffectedAreas:
  - apps/gm
  - docs/planning/v2
  - content model
  - editor integration
dependencies: []
parallelSafety:
  fileOwnership:
    - apps/gm/content/
    - docs/planning/v2/
  requiresInterfaceContract: true
  notes: Parallel execution requires explicit interface contracts and
    non-overlapping generated file ownership.
stories:
  - id: CONTENT-001-S01
    title: A DM or authorized editor shall be able to create, read, update, delete,
      restore, and search markdown notes as the primary content unit of the
      vault
    requirementIds:
      - CONTENT-001
    acceptanceCriteria:
      - Given the user creates a note offline, when saved, then the note is
        durable locally and queued for sync if a remote source is configured.
      - Given an authorized editor reads, updates, deletes, restores, and
        searches the same note, when each command completes, then the visible
        note revision, tombstone/restore state, and search index reflect the
        accepted command.
      - Given a player lacks visibility to a note, when they search or request
        it, then no note content is returned.
      - Given an authorized editor deletes and restores a note, when
        graph/search indexes refresh, then links and search results reflect the
        restored revision without exposing hidden prior content.
      - Given two authorized editors update the same note section concurrently,
        when sync reconciles, then a section-aware merge or durable conflict
        record is produced.
    tasks:
      - id: CONTENT-001-S01-T01
        title: Confirm architecture and interface shape for CONTENT-001
        kind: design
      - id: CONTENT-001-S01-T02
        title: Implement the smallest behavior that satisfies CONTENT-001
        kind: implementation
      - id: CONTENT-001-S01-T03
        title: Add automated coverage for CONTENT-001 acceptance criteria
        kind: test
      - id: CONTENT-001-S01-T04
        title: Record demo notes and traceability evidence for CONTENT-001
        kind: demo
  - id: CONTENT-002-S02
    title: An authorized editor shall be able to edit markdown with visible save
      status, validation feedback, preview, wikilink assistance, and recoverable
      failure states
    requirementIds:
      - CONTENT-002
    acceptanceCriteria:
      - Given an editor is open, when save succeeds, then save status reports
        success and the note revision updates.
      - Given auto-save fails, when the error occurs, then the editor keeps
        unsaved content and shows actionable retry guidance.
    tasks:
      - id: CONTENT-002-S02-T01
        title: Confirm architecture and interface shape for CONTENT-002
        kind: design
      - id: CONTENT-002-S02-T02
        title: Implement the smallest behavior that satisfies CONTENT-002
        kind: implementation
      - id: CONTENT-002-S02-T03
        title: Add automated coverage for CONTENT-002 acceptance criteria
        kind: test
      - id: CONTENT-002-S02-T04
        title: Record demo notes and traceability evidence for CONTENT-002
        kind: demo
qualityBar:
  - "Correctness: every mapped acceptance criterion is implemented or explicitly
    deferred with reviewer-visible evidence."
  - "Architecture integrity: changes obey ADR-014, the architecture contracts,
    package boundaries, and no-v1-runtime-import rules."
  - "Traceability: requirement IDs are tied to code, tests, demo notes, and any
    documentation changes."
  - "Test depth: unit, integration, e2e, boundary, accessibility, performance,
    security, permission, sync, and migration coverage are chosen according to
    risk."
  - "User experience: visible flows are complete, accessible, responsive,
    resilient to empty/error/loading states, and consistent with the design
    system."
  - "Data safety: persistence, offline behavior, sync assumptions, conflict
    handling, actor filtering, and privacy boundaries fail closed."
  - "Maintainability: modules remain cohesive, typed, readable, locally scoped,
    and free of speculative abstractions or unrelated refactors."
  - "Operational quality: diagnostics, metrics, docs, generated files, and
    handoff evidence are current before completion."
gitWorkflow:
  - Start from a clean working tree. Run `git status --short` before editing and
    stop if unrelated changes overlap this epic.
  - Use one branch per epic from the correct base branch, and keep all commits
    scoped to the assigned epic.
  - Never reset, overwrite, or reformat unrelated user changes. Work around
    existing dirty files unless they block the epic.
  - Commit the code, docs, tests, generated workpack updates, and completion
    evidence needed for the epic before handoff.
  - "Leave a clean slate when the epic is complete: no untracked files, no
    unstaged edits, and no stale generated planning diffs caused by the epic."
statusAutomation:
  - Do not hand-edit generated epic packets, `status.yaml`,
    `requirements-index.yaml`, `initiative-map.yaml`, or `parallel-batches.yaml`
    for status changes.
  - Use `pnpm workpack:set-status -- --epic CONTENT-notes-and-editor --status
    active` when implementation starts.
  - Create `docs/planning/v2/epics/CONTENT-notes-and-editor.completion.md` with
    demo, tests, traceability, gaps, and git evidence before marking complete.
  - Use `pnpm workpack:complete -- --epic CONTENT-notes-and-editor` to update
    `workpack-state.yaml` and regenerate all derived planning files together.
  - Run `pnpm workpack:validate` after any status or generation command;
    validation fails if generated files drift from the source of truth.
testPlan:
  - Run unit tests for domain reducers, command validators, and data transforms
    touched by this epic.
  - Run UI or integration checks for any visible prototype behavior touched by
    this epic.
  - Run domain-specific quality gates for accessibility, performance, security,
    permissions, sync/offline behavior, migrations, and docs when those areas
    are touched.
  - Run `pnpm workpack:validate` before handoff.
demoNotesTemplate:
  - Describe the user-visible path demonstrated.
  - List the requirement IDs exercised by the demo.
  - Record any gaps intentionally deferred out of this epic.
stopConditions:
  - Stop if the v2 stack ADR is missing or contradicts the implementation
    approach.
  - Stop if implementing the epic requires runtime imports from v1 app code.
  - Stop if hidden visibility, permission, sync, or persistence behavior is
    ambiguous.
  - Stop if the generated workpack no longer validates.
  - Stop if `git status --short` shows unrelated changes that overlap the files
    this epic needs.
completionEvidence:
  - Targeted tests pass.
  - Traceability from requirement IDs to implementation and tests is documented.
  - Demo notes are filled in for visible behavior.
  - Quality review covers correctness, architecture, security, permissions,
    accessibility, performance, persistence, sync/offline assumptions,
    maintainability, UX polish, and documentation.
  - Completion evidence file records demo path, tests run, changed files,
    requirement coverage, known gaps, git branch, commit or PR, and final `git
    status --short` output.
  - Workpack status is updated with the programmatic complete command after
    completion evidence exists.
  - The epic leaves a clean git slate with no untracked or unstaged files caused
    by the work.
completionEvidenceFile: docs/planning/v2/epics/CONTENT-notes-and-editor.completion.md
