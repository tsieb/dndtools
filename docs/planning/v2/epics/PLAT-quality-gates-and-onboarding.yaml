schemaVersion: 1
id: PLAT-quality-gates-and-onboarding
title: "PLAT: Quality gates and onboarding"
status: approved
approved: true
domain: PLAT
capabilityBranch: Quality gates and onboarding
objective: Deliver the quality gates and onboarding capability branch for PLAT,
  traced to the included v2 requirements.
requirementIds:
  - PLAT-010
  - PLAT-013
  - PLAT-014
  - PLAT-015
excludedRequirementIds: []
architectureContracts:
  - "Contract 1: Processing / Display Decoupling"
  - "Contract 2: Cloud Sync & Offline Model"
  - "Contract 3: Role, Visibility & Permission Grant Model"
  - "Contract 4: Scene and Widget Contract"
expectedAffectedAreas:
  - apps/v2
  - docs/planning/v2
  - platform services
  - runtime shell
dependencies: []
parallelSafety:
  fileOwnership:
    - apps/v2/plat/
    - docs/planning/v2/
  requiresInterfaceContract: true
  notes: Parallel execution requires explicit interface contracts and
    non-overlapping generated file ownership.
stories:
  - id: PLAT-010-S01
    title: Release and platform quality gates shall be tiered, high-value, owned,
      and bounded by configured time budgets that preserve developer feedback
      without allowing platform-critical regressions
    requirementIds:
      - PLAT-010
    acceptanceCriteria:
      - Given a change affects platform code, when CI runs, then the relevant
        platform smoke or release gate is selected by path/tier rules.
      - Given a gate exists, when reviewed, then it has an owner, reason, and
        user-facing defect class it protects.
      - Given the smoke path runs on supported CI hardware, when measured, then
        it completes under the configured target of three minutes or records an
        explicit scope exception.
      - Given a gate has not caught or prevented a user-facing defect class for
        the configured review window, when quality review runs, then the gate is
        removed, narrowed, or re-justified.
    tasks:
      - id: PLAT-010-S01-T01
        title: Confirm architecture and interface shape for PLAT-010
        kind: design
      - id: PLAT-010-S01-T02
        title: Implement the smallest behavior that satisfies PLAT-010
        kind: implementation
      - id: PLAT-010-S01-T03
        title: Add automated coverage for PLAT-010 acceptance criteria
        kind: test
      - id: PLAT-010-S01-T04
        title: Record demo notes and traceability evidence for PLAT-010
        kind: demo
  - id: PLAT-013-S02
    title: Fresh-vault onboarding, feature-tier visibility, maturity gates, help
      surfaces, and first-run Command Center setup shall be covered by
      fixture-driven acceptance tests
    requirementIds:
      - PLAT-013
    acceptanceCriteria:
      - Given a fresh vault fixture, when first-run onboarding completes, then
        the Command Center, core navigation, and feature-tier state match
        expected defaults.
      - Given core/intermediate/advanced feature gates are configured, when
        fixture tests run, then each tier shows and hides the correct
        capabilities without manual verification.
    tasks:
      - id: PLAT-013-S02-T01
        title: Confirm architecture and interface shape for PLAT-013
        kind: design
      - id: PLAT-013-S02-T02
        title: Implement the smallest behavior that satisfies PLAT-013
        kind: implementation
      - id: PLAT-013-S02-T03
        title: Add automated coverage for PLAT-013 acceptance criteria
        kind: test
      - id: PLAT-013-S02-T04
        title: Record demo notes and traceability evidence for PLAT-013
        kind: demo
  - id: PLAT-014-S03
    title: Platform support status for desktop, web/PWA, Android, tablet, and mobile
      profiles shall be declared before release with explicit parity,
      degradation, and unsupported-feature lists
    requirementIds:
      - PLAT-014
    acceptanceCriteria:
      - Given a Must-have command is unsupported on a platform profile, when
        release review runs, then the release is blocked unless the requirement
        explicitly allows that exception.
      - Given a platform degrades a feature, when the user opens capability
        status, then the reason and available fallback are visible.
    tasks:
      - id: PLAT-014-S03-T01
        title: Confirm architecture and interface shape for PLAT-014
        kind: design
      - id: PLAT-014-S03-T02
        title: Implement the smallest behavior that satisfies PLAT-014
        kind: implementation
      - id: PLAT-014-S03-T03
        title: Add automated coverage for PLAT-014 acceptance criteria
        kind: test
      - id: PLAT-014-S03-T04
        title: Record demo notes and traceability evidence for PLAT-014
        kind: demo
  - id: PLAT-015-S04
    title: Engineering release notes, structure inventories, and defect-count
      summaries shall be generated or validated from structured sources rather
      than hand-synchronized markdown counts
    requirementIds:
      - PLAT-015
    acceptanceCriteria:
      - Given the defect register changes, when documentation validation runs,
        then summary counts match structured finding data.
      - Given repository structure changes, when docs validation runs, then
        stale generated structure references are reported.
      - Given `10-requirements.md` contains a Count Audit table, when docs
        validation runs, then counts are recomputed from requirement headings
        and the table fails validation if hand-synchronized values drift.
    tasks:
      - id: PLAT-015-S04-T01
        title: Confirm architecture and interface shape for PLAT-015
        kind: design
      - id: PLAT-015-S04-T02
        title: Implement the smallest behavior that satisfies PLAT-015
        kind: implementation
      - id: PLAT-015-S04-T03
        title: Add automated coverage for PLAT-015 acceptance criteria
        kind: test
      - id: PLAT-015-S04-T04
        title: Record demo notes and traceability evidence for PLAT-015
        kind: demo
qualityBar:
  - "Correctness: every mapped acceptance criterion is implemented or explicitly
    deferred with reviewer-visible evidence."
  - "Architecture integrity: changes obey ADR-014, the architecture contracts,
    package boundaries, and no-v1-runtime-import rules."
  - "Traceability: requirement IDs are tied to code, tests, demo notes, and any
    documentation changes."
  - "Test depth: unit, integration, e2e, boundary, accessibility, performance,
    security, permission, sync, and migration coverage are chosen according to
    risk."
  - "User experience: visible flows are complete, accessible, responsive,
    resilient to empty/error/loading states, and consistent with the design
    system."
  - "Data safety: persistence, offline behavior, sync assumptions, conflict
    handling, actor filtering, and privacy boundaries fail closed."
  - "Maintainability: modules remain cohesive, typed, readable, locally scoped,
    and free of speculative abstractions or unrelated refactors."
  - "Operational quality: diagnostics, metrics, docs, generated files, and
    handoff evidence are current before completion."
gitWorkflow:
  - Start from a clean working tree. Run `git status --short` before editing and
    stop if unrelated changes overlap this epic.
  - "Create a fresh branch for this epic off the main v2 branch
    `v2-clean-slate`: `git checkout v2-clean-slate && git pull && git checkout
    -b epic/PLAT-quality-gates-and-onboarding`. Keep all commits scoped to the
    assigned epic."
  - Never reset, overwrite, or reformat unrelated user changes. Work around
    existing dirty files unless they block the epic.
  - Commit the code, docs, tests, generated workpack updates, and completion
    evidence needed for the epic before handoff.
  - "On completion, merge the epic branch back into the main v2 branch and push
    the result: `git checkout v2-clean-slate && git pull && git merge --no-ff
    epic/PLAT-quality-gates-and-onboarding && git push origin v2-clean-slate`."
  - "Leave a clean slate when the epic is complete: no untracked files, no
    unstaged edits, and no stale generated planning diffs caused by the epic."
statusAutomation:
  - Do not hand-edit generated epic packets, `status.yaml`,
    `requirements-index.yaml`, `initiative-map.yaml`, or `parallel-batches.yaml`
    for status changes.
  - Use `pnpm v2:workpack:set-status -- --epic PLAT-quality-gates-and-onboarding
    --status active` when implementation starts.
  - Create
    `docs/planning/v2/epics/PLAT-quality-gates-and-onboarding.completion.md`
    with demo, tests, traceability, gaps, and git evidence before marking
    complete.
  - Use `pnpm v2:workpack:complete -- --epic PLAT-quality-gates-and-onboarding`
    to update `workpack-state.yaml` and regenerate all derived planning files
    together.
  - Run `pnpm v2:workpack:validate` after any status or generation command;
    validation fails if generated files drift from the source of truth.
testPlan:
  - Run unit tests for domain reducers, command validators, and data transforms
    touched by this epic.
  - Run UI or integration checks for any visible prototype behavior touched by
    this epic.
  - Run domain-specific quality gates for accessibility, performance, security,
    permissions, sync/offline behavior, migrations, and docs when those areas
    are touched.
  - Run `pnpm v2:workpack:validate` before handoff.
demoNotesTemplate:
  - Describe the user-visible path demonstrated.
  - List the requirement IDs exercised by the demo.
  - Record any gaps intentionally deferred out of this epic.
stopConditions:
  - Stop if the v2 stack ADR is missing or contradicts the implementation
    approach.
  - Stop if implementing the epic requires runtime imports from v1 app code.
  - Stop if hidden visibility, permission, sync, or persistence behavior is
    ambiguous.
  - Stop if the generated workpack no longer validates.
  - Stop if `git status --short` shows unrelated changes that overlap the files
    this epic needs.
completionEvidence:
  - Targeted tests pass.
  - Traceability from requirement IDs to implementation and tests is documented.
  - Demo notes are filled in for visible behavior.
  - Quality review covers correctness, architecture, security, permissions,
    accessibility, performance, persistence, sync/offline assumptions,
    maintainability, UX polish, and documentation.
  - Completion evidence file records demo path, tests run, changed files,
    requirement coverage, known gaps, git branch, commit or PR, and final `git
    status --short` output.
  - Workpack status is updated with the programmatic complete command after
    completion evidence exists.
  - The epic leaves a clean git slate with no untracked or unstaged files caused
    by the work.
