schemaVersion: 1
id: GRAPH-backlinks-and-navigation-relationships
title: 'GRAPH: Backlinks and navigation relationships'
status: approved
approved: true
domain: GRAPH
capabilityBranch: Backlinks and navigation relationships
objective: Deliver the backlinks and navigation relationships capability branch
  for GRAPH, traced to the included v2 requirements.
requirementIds:
  - GRAPH-002
excludedRequirementIds: []
architectureContracts:
  - 'Contract 3: Role, Visibility & Permission Grant Model'
expectedAffectedAreas:
  - apps/v2
  - docs/planning/v2
  - graph index
  - relationship intelligence
dependencies: []
parallelSafety:
  fileOwnership:
    - apps/v2/graph/
    - docs/planning/v2/
  requiresInterfaceContract: true
  notes: Parallel execution requires explicit interface contracts and
    non-overlapping generated file ownership.
stories:
  - id: GRAPH-002-S01
    title: A user shall be able to inspect backlinks, cross-section links, and
      related-note jumps from visible content with snippets redacted according
      to visibility
    requirementIds:
      - GRAPH-002
    acceptanceCriteria:
      - Given a visible note has three backlinks, when the user opens backlinks,
        then visible backlinks and context snippets appear.
      - Given a backlink source is hidden, when a player opens backlinks, then
        that backlink is absent.
    tasks:
      - id: GRAPH-002-S01-T01
        title: Confirm architecture and interface shape for GRAPH-002
        kind: design
      - id: GRAPH-002-S01-T02
        title: Implement the smallest behavior that satisfies GRAPH-002
        kind: implementation
      - id: GRAPH-002-S01-T03
        title: Add automated coverage for GRAPH-002 acceptance criteria
        kind: test
      - id: GRAPH-002-S01-T04
        title: Record demo notes and traceability evidence for GRAPH-002
        kind: demo
testPlan:
  - Run unit tests for domain reducers, command validators, and data transforms
    touched by this epic.
  - Run UI or integration checks for any visible prototype behavior touched by
    this epic.
  - Run `pnpm v2:workpack:validate` before handoff.
demoNotesTemplate:
  - Describe the user-visible path demonstrated.
  - List the requirement IDs exercised by the demo.
  - Record any gaps intentionally deferred out of this epic.
stopConditions:
  - Stop if the v2 stack ADR is missing or contradicts the implementation
    approach.
  - Stop if implementing the epic requires runtime imports from v1 app code.
  - Stop if hidden visibility, permission, sync, or persistence behavior is
    ambiguous.
  - Stop if the generated workpack no longer validates.
completionEvidence:
  - Targeted tests pass.
  - Traceability from requirement IDs to implementation and tests is documented.
  - Demo notes are filled in for visible behavior.
  - Workpack status is updated after completion.
