# CI recovery for 27e419688943

## Scope and starting state

- Task: `ci-recovery-27e419688943c189a61e9f5a47c5066c8b059929`.
- Clean task branch `dispatch/dndtools/c90d1aed6802c4c2bc2b` started at the reported SHA.
- Repair and commit locally; no push, promotion, loop, additional agents, or dispatcher control-state edits.

## Investigation

- GitHub CI run [35450743832](https://github.com/tsieb/dndtools/actions/runs/35450743832)
  fails only the golden-route visual regression job. Build/unit, three browser shards,
  Android, Electron, and accessibility jobs pass.
- Retrieved original failed-job output: 18 screenshot mismatches, covering `/board`
  and `/scene/:id` in every theme and desktop/rail/phone tier; 117 tests pass.
- The second CI run, [35450740899](https://github.com/tsieb/dndtools/actions/runs/35450740899),
  likewise fails only visual regression.

## Reproduction and cause

- `CI=1 DNDTOOLS_PW_WORKERS=2 bash apps/gm-react/tests/visual/run-in-container.sh --update-snapshots=none --grep '/board|/scene/:id' --reporter=line --retries=0`
  exits 1 with all 18 reported comparisons failing. Desktop tavern `/board` reproduces
  the runner's exact 1,853-pixel difference.
- The pinned image is `mcr.microsoft.com/playwright:v1.61.1-noble@sha256:5b8f294aff9041b7191c34a4bab3ac270157a28774d4b0660e9743297b697e48`.
- These baselines predate RC-ENG-8.2 (`c930cd99` and `c8a2c291`). The default-content
  repair binds the home Map tile to Ruined Keep and the seeded scene's Character tile
  to Brother Calloway. The old screenshots show a missing-map warning and empty
  character values; the application correctly renders the map and character now.
- Inspected actual/diff images and all 18 captures in desktop/rail/phone contact sheets.
  The intended differences are confined to the map and character tiles. Existing
  viewport clipping and canvas zoom/layout are unchanged.
- Downloaded the original CI image artifacts for comparison. Sixteen local actual PNGs
  are byte-identical to CI. The other two differ only in the desktop high-contrast top-bar
  icon and a 2-by-5-pixel phone canvas edge region, outside the changed tile content.
- `HEAD`, `loop/rc`, and `origin/loop/rc` all resolve to the reported SHA at verification.

## Repair

- Re-rendered only `/board` and `/scene/:id` with the same pinned container and
  `--update-snapshots=changed`. Exactly 18 PNGs changed, across three themes and three tiers.
- No application code, screenshot assertions, tolerance, fixture, test selection, or
  workflow protection is changed. The existing 40-pixel allowance remains intact.

## Verification

- Full visual gate: **135/135 pass**, 45 per layout tier, exit 0 in 2.4 minutes.
  `CI=1 DNDTOOLS_PW_WORKERS=2 bash apps/gm-react/tests/visual/run-in-container.sh --update-snapshots=none --reporter=line --retries=0`
  The complete original log was retrieved; `.last-run.json` also reports `passed` with
  no failed tests. No baselines were rewritten by this verification run.
- App seed and widget-body tests: 33/33 pass.
  `pnpm exec vitest run --config vitest.app.config.ts apps/gm-react/src/runtime/demo-seed.test.ts apps/gm-react/src/app/widgets/builtin/builtin-bodies.test.tsx`
- Core default binding tests: 9/9 pass.
  `pnpm --filter @dndtools/core exec vitest run tests/command-center-default-bindings.test.ts`
- Baseline budget passes: 135 files, 12,832.8 KiB of 32,768 KiB.
  `node apps/gm-react/tests/visual/check-baseline-budget.mjs`
- `pnpm audit:repo`: all 14 CI guardrail tests pass.
- `pnpm gates`: passes; six gates wired and 257 documentation files reachable.
  Its 55 file-size warnings concern unchanged source files.
- The first complete visual-suite attempt hit Headroom's hard 120-second command timeout;
  it is not counted as a gate pass. Restarted the full unchanged comparison in a native
  process, with exact output retained in `tmp/ci-recovery-27e419688943/full-visual.log`.
- Unit output includes existing Vite config-loader and React `act` warnings, with no failures.

## Exact diagnostic artifacts

Original output was retrieved through Headroom before drawing conclusions:

- Remote failed job: `f58e800491dc42d3840b5f5238311b73` (386,655 bytes).
- Local 18-failure reproduction: `f0b88757924146a9bc6a46a0ef9c194b` (60,335 bytes).
- Targeted baseline generation: `f688bc1009d2470fa2e190b6f80f48bd` (18 generated captures).
- App unit tests: `ec0a494384674f8da23ea94e8cb615fc`; core bindings:
  `8a931a235e774d7e879acd413e8bccef`; CI guardrails: `5f017422c7f946ab800fe631a16185a7`.
- Quality gates: `269cde1aa3064be6a8ce475c4939781b`.
- Full visual comparison: `a956a85930ae4d79a30a7285f73c2aef` (15,716 bytes), SHA-256
  `5cf6a95134b56d56444c66b927819e7a071f578b5b2e788652c087ad7c72b776`.

Local reproduction screenshots, downloaded CI artifacts, and review contact sheets are under
the ignored `tmp/ci-recovery-27e419688943/` directory. They are diagnostic artifacts only;
the intended commit contains the 18 reviewed baseline PNGs and this journal.

## Original handoff (before integration reconciliation)

The reported failure is reproduced and repaired on the current integration base. Local visual,
binding, guardrail, size-budget, quality, formatting, and whitespace checks pass. GitHub has not
run this local candidate; the central operator owns subsequent gates, independent review, and
delivery. No push, promotion, loop, or dispatcher control-state change was performed.

## Integration reconciliation — 2026-09-20

- Resumed from `6c8dff3d` after the operator reported two binary rebase conflicts.
- Rechecked GitHub run `35450743832` at the reported SHA: only the visual regression
  job failed. Downloaded its original failed-job log again to
  `tmp/ci-recovery-27e419688943/github-failed-original.log`.
- Rebasing onto `2d9f566d194d10e597c8001015b7e8be31811d59` reproduced exactly the
  reported desktop scene-editor high-contrast and parchment conflicts.
- Integration already contains the same binding repair in `989061ed`, documented in
  `state/ci-visual-repair-2026-09-19.journal.md`. Sixteen of this task's eighteen
  baselines are byte-identical to integration. Image comparison of the remaining
  two shows the same bound character and layout, with visible differences around
  the top-bar icon. Retained integration's versions of both conflicted PNGs.
- All newer map-editor and Settings baselines and all integration source changes
  are preserved. The reconciled task has no runtime, assertion, workflow, or PNG
  difference from the integration base; this journal records the recovery evidence.
- Full pinned-container visual comparison on this combined candidate: **135/135
  passed**, exit 0, in 2.4 minutes, with `CI=1`, two workers,
  `--update-snapshots=none`, and `--retries=0`. Read the complete original output;
  `.last-run.json` reports `passed` and no failed tests. Exact output is retained in
  `tmp/ci-recovery-27e419688943/reconciliation/full-visual.log`, SHA-256
  `5cf6a95134b56d56444c66b927819e7a071f578b5b2e788652c087ad7c72b776`.
- Re-ran the same app seed/widget-body tests (33/33), core default binding tests
  (9/9), and CI guardrails (14/14): all pass. Original app and core output is in
  `reconciliation/app-bindings.log` and `reconciliation/core-bindings.log` beneath
  the same diagnostic directory. Baseline budget: 135 files, 12881.2 KiB of
  32768.0 KiB; passes.
- `pnpm gates` passes: six gates wired and 257 documentation files reachable.
  Original output is in `reconciliation/quality-gates.log`; only existing source
  size warnings occur. Journal Prettier and whitespace checks pass.
- Rebase completed on the original task branch. The final commit records this
  reconciliation and verification; integration already carries the required PNG
  repair. No tests, thresholds, workflow protections, or unrelated changes were
  weakened or overwritten. No push, promotion, loop, or dispatcher control-state
  edits were performed. Remote CI and independent review remain the operator's
  next steps.
