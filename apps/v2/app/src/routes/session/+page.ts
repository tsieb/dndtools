// SES-002 / SES-006: the Session section. Client-rendered like the rest of the prototype (ADR-014:
// client/static, no server-owned state); the runtime + actor-filtered queries run in the browser.
export const ssr = false;
export const prerender = false;
