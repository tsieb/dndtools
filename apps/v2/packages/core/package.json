{
	"name": "@dndtools/v2-core",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"description": "v2 Processing Core: commands, reducers, permissions, queries, sync log shape.",
	"main": "./src/index.ts",
	"types": "./src/index.ts",
	"exports": {
		".": "./src/index.ts",
		"./testing": "./src/testing/index.ts"
	},
	"scripts": {
		"build": "tsc -p tsconfig.json --noEmit",
		"typecheck": "tsc -p tsconfig.json --noEmit",
		"test": "vitest run"
	},
	"devDependencies": {
		"typescript": "^6.0.3",
		"vitest": "^4.1.8",
		"zod": "^4.4.3"
	}
}
