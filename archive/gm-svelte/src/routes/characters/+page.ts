// CHAR-001 / CHAR-002 / CHAR-013: the Characters section. Client-rendered like the rest of the
// prototype (ADR-014: client/static, no server-owned state); the runtime + actor-filtered queries
// run in the browser.
export const ssr = false;
export const prerender = false;
