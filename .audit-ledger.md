# Widget Platform Unification — UX/UI Audit Ledger

Branch: `feat/unify-widget-platform`. Scope: ONLY the branch diff (widget-platform-unification).
Baseline (must stay green every round): core 3125 + gui 509 unit, full `pnpm lint`, `pnpm docs:validate`, full Playwright (desktop-chromium + mobile-chromium).

## Convergence rule
A finding is **actionable** only if it is (a) spec-backed with a `UX-*` req ID, or (b) a concrete broken interaction path / bug. Tag every finding: severity [blocker|major|minor|polish] + req ID (or `no-spec`).
The loop terminates when a round yields only: subjective polish with no spec ID, pre-existing issues outside the branch diff, or documented intentional divergences.

## KNOWN-INTENTIONAL — DO NOT FLAG, DO NOT "FIX"
1. **Compact/mobile bespoke renderers** kept inline for testid stability: `SessionWidget` always-visible status strip rendered outside the loading branch; `ToolsWidget` desktop grid vs compact Contract-1 tablist. These intentional divergences are pinned by mobile E2E + testid selectors.
2. **GUI catalog vs core `listWidgetLibrary` divergence is intentional**: `map` desktop/tablet-only is a CMD-005 fixture asserted in `apps/gm/tests/unit/ux-canvas-library.test.ts` (unsupported widgets kept but `available:false`). If unifying, reconcile INTO core; never overwrite GUI to match core blindly.

---

## Round 1 findings (triaged) — baseline green: core 3125 / gui 509 / lint / docs

### Group A — render layer (templates/*, AudioWidget, CustomWidgetFrame, WidgetView, widget-data.ts)
- A1 [blocker UX-VIS-012/A11Y-001] white-on-accent contrast ~2.2:1 — TemplateActionPanel:89, AudioWidget:62 → use accent-foreground token
- A2 [major UX-VIS] data-table/chart ignore resolver emptyLabel (TemplateDataTable, TemplateChart)
- A3 [major UX-VIS-004] body <13px: TemplateDataTable --text-xs primary; --text-2xs metadata overuse
- A4 [major principle7/UX-AUDIO-002] AudioWidget toggle fakes playback (animated EQ, no real effect)
- A5 [minor 1.3.1] TemplateDataTable th missing aria-sort
- A6 [minor A11Y-007] TemplateStatusList aria-label on non-semantic span
- A7 [minor principle7] CustomWidgetFrame no empty/error fallback (blank iframe)
- A8 [minor UX-VIS-013] inconsistent primary-button emphasis across templates
- A9 [minor] data-table horizontal overflow on mobile (no truncation)
- A10 [minor UX-VIS-010/A11Y-011] TemplateTracker transition not reduced-motion gated; raw px
- A11 [polish] WidgetView placeholder dev-ish text

### Group B — customize/properties (CanvasPropertiesPanel, WidgetCustomizePanel, +new shared field cmp; core widget-package-state.ts, widget-package-review.ts)
- B1 [major A11Y-012] CanvasPropertiesPanel tabs: no role=tabpanel/aria-controls/id, no arrow-key nav
- B2 [major UX-CANVAS-007/principle7] color swatch shows #888 gray for un-overridden tokens (both panels)
- B3 [major UX-VIS consistency] two forked panels — extract shared field-renderer (PRESERVE testids) or align+fix docstring
- B4 [major/minor] WidgetCustomizePanel oninput per-keystroke dispatch → onchange (match CC)
- B5 [minor] field.help dropped in CC panel; B's help inside <label> leaks a11y name (scene)
- B6 [major no-spec] CanvasPropertiesPanel .props-body overflow clip → overflow-y:auto
- B7 [minor] "Theme" reset label unclear → "Reset to theme default" + aria-label (both)
- B8 [minor] numeric fields don't clamp min/max on commit
- B9 [minor] definition.description never surfaced (schema says it should be)
- B10 [minor #13] surface style token is a no-op for system widgets → drop from defaultWidgetStyle()
- B11 [blocker, core preview] widget-package-review.ts:288 same white-on-accent contrast

### Group C — scene/maps/atlas + cross-surface chrome (scene/+page, CanvasViewport, AtlasWidget, MapWidget, routes/+page, Session/PlayerViews/Tools/GettingStarted widgets, styles.css, DashboardBlock)
- C1 [major UX-CANVAS-011 BOUNDARY] preview renders raw DM-resolved widget bodies (scene tileBody) → gate on !previewActive (no-leak)
- C2 [major UX-CANVAS-007/008] MapWidget shows maps[0] as if bound; unbound state unreachable
- C3 [major UX-CANVAS-007] cross-surface chrome drift: scene shows raw tile.type vs CC displayName (CanvasViewport ~795)
- C4 [minor] AtlasWidget dropped actor filtering (listMapsForActor) — latent redaction regression
- C5 [minor] AtlasWidget "Projecting" state has no CSS ([data-projecting]/.projection-state)
- C6 [minor principle6] AtlasWidget primary emphasis on "Set active map" not "Project to players"
- C7 [minor] AtlasWidget .active-map-section unstyled
- C8 [minor 1.3.1] nested <h2> under block title: Atlas/PlayerViews/Tools/Session widgets → demote to h3
- C9 [minor UX-MAP-010] MapWidget + Atlas thumbnails link bare /atlas/ → /atlas/?map=<id>
- C10 [minor] Project/Queue disabled with no reason hint

### LOGGED — pre-existing / out of scope (do NOT fix this round)
- P1 [UX-CMD-003] CC status strip not pinned (pre-existing redesign)
- P2 [UX-CMD-010] CC raw workflow strip lacks confirm on end/archive — carries unique info+testid; needs product decision
- P3 [UX-CMD-008/009] presets/library cramped in one tile (pre-existing redesign)
- P4 [UX-CMD-004] player rows dense (pre-existing)

### DEFERRED enhancements (note, not bugs)
- aspect-ratio lock + X/Y in scene panel (UX-CANVAS-003 Should-have)
- make --widget-surface functional (paint background) — feature, has visual/test risk
- render filtered preview bodies (vs gating) — needs previewed-actor data plumbing

## Round 1 fixes applied (3 file-disjoint agents)
Group A (render layer): A1 contrast→accent-foreground var; A2 emptyLabel consumed (table/chart); A3 fonts ≥--text-sm, no --text-2xs in owned files; A4 AudioWidget now honest read-only glance (removed fake playing/EQ); A5 aria-sort; A6 badge role=img; A7 CustomWidgetFrame empty fallback; A8 one filled primary per template; A9 cell truncation; A10 reduced-motion gate + px→tokens; A11 placeholder copy softened. +test widget-honest-feedback.test.ts (8).
Group B (customize): B1 tabs ARIA+roving via nextRovingIndex primitive; B2 honest "Theme default/Custom" state vs gray swatch; B3 ALIGNED in-place (extraction impossible — divergent testid prefixes + toggle/color DOM), docstring corrected; B4 oninput→onchange; B5 help outside label + aria-describedby (both); B6 .props-body overflow-y:auto; B7 reset label+aria-label; B8 numeric clamp; B9 description subtitle; B10 dropped no-op surface token; B11 core preview contrast. +tests: core widget-platform-unification (token set), custom-widget-platform (preview contrast), gui widget-customize-panels (8).
Group C (scene/maps/chrome): C1 preview body gated on !previewActive (no DM leak); C2 MapWidget unbound state (dropped maps[0]); C3 scene chrome shows displayName via tileTypeLabel prop; C4 AtlasWidget actor-filtered maps; C5 [data-projecting] glanceable CSS; C6 primary=Project to players; C7 .active-map-section card; C8 inner h2→h3 (Atlas/PlayerViews/Tools/Session); C9 /atlas/?map= deep-links; C10 disabled-reason hints.

### Cross-agent flags to verify in gate
- Group C saw transient typecheck err in Group A's test (stale — test passes definition: def). VERIFY.
- styles.css 0.9em token failure introduced+fixed by Group C. VERIFY theme-tokens.test green.
- B left CC-panel control-height (--touch-target-floor) per-surface density on purpose.

### Round 1 gate: FULLY GREEN (exit 0) — typecheck 0/0, core 3127, gui 521, eslint+boundary+nav+a11y-contrast, docs, Playwright 815 passed/39 skipped (desktop+mobile). 31 findings fixed.

---

## Round 2 (re-audit for regressions/misses + dedicated test-thoroughness pass)
### Findings (Round 2 — did NOT converge)
Test pass: +43 tests (core 3143, gui 540, +2 e2e incl. preview no-leak + clamp); NO source bugs found.
Re-audit render/customize: RA1-M1 scene Customize Reset button below 24px floor (gated behind override, gate misses); RA1-m2 Size W/H not clamped like commitRect; RA1-m3 tracker buttons --text-xs lone outlier; RA1-m4 token color aria-label uses slug while reset uses description (two names, one token); RA1-m5 accent-foreground not a token (dark-accent override → low contrast). NO regressions; roving nav/audio honesty/token-state verified clean.
Re-audit scene/maps/CC: RA2-M1 scene CARD-LIST title still raw w.type (canvas head fixed, card list missed, scene/+page:1329); RA2-M2 preview now renders BLANK bodies (Round-1 gate over-corrected); RA2-m3 compact active-map emphasis not mirrored (Project not primary on mobile) + title-tooltip not touch-visible; RA2-m4 MapWidget region chips --text-2xs (10px). Verified clean: tileTypeLabel wiring, no .app-main overflow, heading outline, deep-links, actor-filtered atlas.

### Round 3 plan (apply directly — small coupled batch; advisor: no fan-out re-spin)
1 RA1-M1 .customize-reset min-height:--touch-target-min
2 RA1-m2 Size W/H clamped commit
3 RA1-m3 TemplateTracker .tpl-actions button --text-sm
4 RA1-m4 color input + reset SAME concise a11y name (not description prose)
5 RA2-M1 scene card-list title def.displayName
6 RA2-M2 preview body → neutral "hidden in preview" affordance (leak-free, intentional)
7 RA2-m3 compact active-map: Project=primary + VISIBLE disabled hint; AtlasWidget desktop also visible hint (consistency)
8 RA2-m4 MapWidget .map-regions --text-xs
DROPPED: RA1-m5 accent-foreground knob (complexity-outward; A1 fallback already fixes default themes) → logged as known color-picker limitation.
DEFERRED: full viewer-scoped preview rendering (needs MapWidget region actor-filtering + context wrapper); MapWidget raw-region read (only reachable via preview, now gated).
GUARDRAIL: re-run FULL gate incl. BOTH Playwright projects (mobile .app-main overflow risk on compact hint).

### Round 3 fixes applied (direct, by orchestrator)
1 RA1-M1 WidgetCustomizePanel .customize-reset min-height:--touch-target-min (+font --text-xs)
2 RA1-m2 WidgetCustomizePanel commitWidth/commitHeight clamp to minSize, guard empty/NaN
3 RA1-m3 TemplateTracker .tpl-actions button --text-xs→--text-sm
4 RA1-m4 CanvasPropertiesPanel token reset aria-label: description-prose→concise token.name (matches Customize panel)
5 RA2-M1 scene/+page:1329 card-list title {w.type}→{def?.displayName ?? w.type}; updated scene-create.spec ('initiative-tracker'→'Initiative Tracker')
6 RA2-M2 scene tileBody preview: neutral ".canvas-widget-preview" placeholder ("Content hidden in preview") instead of blank; +styles
7 RA2-m3 compact (+page) active-map: Project=primary(.button)/Set active map=secondary; +VISIBLE projectionDisabledHint; AtlasWidget desktop also visible hint; .active-map-hint in styles.css
8 RA2-m4 MapWidget .map-regions --text-2xs→--text-xs
DROPPED RA1-m5 (accent-foreground knob). DEFERRED viewer-scoped preview render + MapWidget region actor-filter.

### Round 3 gate: FULLY GREEN — typecheck 0/0, core 3143, gui 540, lint, docs, Playwright 819 passed/39 skipped (both projects). Test fixups: scene-create.spec card titles → display names ('Initiative Tracker', 'Weather Panel' scoped to <strong> to avoid placeholder dup).

## Convergence re-audit (focused, Round 3 touched files)
7/8 files CONFIRMED CLEAN. One real finding I introduced: `.canvas-widget-preview` placeholder used --color-text-tertiary (~3.4:1, < AA). FIXED → --color-text-secondary (~5.8:1, matches passing .active-map-hint). Re-verified typecheck+lint.

## TERMINATION: CONVERGED.
Remaining items are only: DEFERRED enhancements (viewer-scoped preview rendering; MapWidget region actor-filter; free-form accent contrast) + LOGGED pre-existing CC redesign (status-strip pinning, raw workflow strip confirm, cramped presets tile, dense player rows) + intentional divergences. No actionable spec-backed/broken-interaction findings remain in the branch diff.

Totals across 3 rounds: 40 findings fixed; tests +43 (core 3125→3143, gui 509→540, e2e +2); full gate green throughout.
